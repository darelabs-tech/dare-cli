# Task mp035-002: Parse + feature-gated grammars

> **Complexidade:** HIGH · **Depends on:** mp035-001 · **Fase:** 2  
> **BLUEPRINT-035** T-03 T-04 T-05 T-17

## Objetivo

Integrar tree-sitter 0.25.10 e grammars sob feature flags `lang-*`.

## Arquivos

- `crates/dare-ast/Cargo.toml` (features + deps)
- `crates/dare-ast/src/parse.rs` (NOVO)

## Assinaturas (MUST)

```rust
pub fn grammar_available(lang: Language) -> bool;
pub fn parse_source(lang: Language, source: &str) -> Result<tree_sitter::Tree, ParseError>;
```

## Features (MUST)

`lang-typescript`, `lang-tsx`, `lang-javascript`, `lang-python`, `lang-php`, `lang-go`, `lang-ruby`, `lang-rust`; default = all.

## Validation Gates

- [ ] `cargo test -p dare-ast --all-features`
- [ ] `cargo check -p dare-ast --no-default-features`

## Definition of Done

- [ ] Parse OK para snippet mínimo por lang (quando feature on)
- [ ] `grammar_available` false quando feature off
