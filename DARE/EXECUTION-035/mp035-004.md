# Task mp035-004: Regex fallback + merge/dedupe

> **Complexidade:** HIGH · **Depends on:** mp035-003 · **Fase:** 4  
> **BLUEPRINT-035** T-08 T-09

## Objetivo

Fallback regex + merge preferindo AST; ordenação determinística.

## Arquivos

- `crates/dare-ast/src/regex_fallback.rs` (NOVO)
- `crates/dare-ast/src/merge.rs` (NOVO)

## Assinaturas (MUST)

```rust
pub fn extract_regex(lang: Language, source: &str) -> (Vec<HttpEndpoint>, Vec<Entity>);
pub fn merge_extractions(ast: ..., regex: ...) -> (Vec<HttpEndpoint>, Vec<Entity>);
```

## Validation Gates

- [ ] `cargo test -p dare-ast --no-default-features`
- [ ] `cargo test -p dare-ast --all-features`

## Definition of Done

- [ ] Regex extrai sem grammar
- [ ] Dedupe: key endpoint METHOD\\0path; entity kind\\0name; prefer Ast
- [ ] Sort estável
