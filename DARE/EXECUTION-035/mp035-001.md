# Task mp035-001: Scaffold crate + model/language

> **Complexidade:** MED · **Depends on:** — · **Fase:** 1  
> **BLUEPRINT-035** T-01 T-02 T-10 T-14 §3–4

## Objetivo

Criar `crates/dare-ast` com tipos de domínio e detecção de linguagem.

## Arquivos

- `crates/dare-ast/Cargo.toml` (NOVO)
- `crates/dare-ast/src/{lib,model,language}.rs` (NOVO)
- `Cargo.toml` workspace members + `dare-ast` path dep

## Assinaturas (MUST)

```rust
pub enum Language { TypeScript, Tsx, JavaScript, Python, Php, Go, Ruby, Rust }
pub fn detect_language(path: &str) -> Option<Language>;
pub const MAX_SOURCE_BYTES: usize = 2 * 1024 * 1024;
```

## Validation Gates

- [ ] `cargo test -p dare-ast`
- [ ] Sem tree-sitter ainda · Sem CLI

## Definition of Done (ANTI-STUB)

- [ ] Tipos Serialize + Eq
- [ ] Testes: `.ts`→TS, `.tsx`→Tsx, `.js`→JS, `.py`→Python, `.php`→Php, `.go`→Go, `.rb`→Ruby, `.rs`→Rust, unknown→None
- [ ] Sem deps `dare-cli`
