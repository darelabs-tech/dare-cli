# Task mp013-004: CLI help `--force` + install pipeline

## Objetivo

Fase 4 — help force + ordem install.

## Alterações

- `CodexCmd::Install`: docstring `--force` — unmanaged overwritten when set
- Ordem: `generate_agents_md` → `install_codex_skills`

## Arquivos

- `crates/dare-cli/src/main.rs`

## Validation Gates

- [ ] `cargo build -p dare-cli`

## Definition of Done (ANTI-STUB)

- [ ] Help `--force` explícito
- [ ] Sem git commit
