# Task mp013-003: install_codex_skills + validate + coexistência

## Objetivo

Fase 3 — 49 skills + share `.agents` + preserve.

## Assinaturas

```rust
pub fn install_codex_skills(root: &ProjectRoot, force: bool) -> CoreResult<usize>;
pub fn validate_codex_install(root: &ProjectRoot) -> CoreResult<usize>;
```

## Regras

- Body: managed + `render_agent_skill`
- Dual write: `outputs.codex` + `.agents/skills/{id}/SKILL.md`
- force → 49; shared unmanaged preservado
- validate exige AGENTS.md + paths

## Testes

- roundtrip 49 + shared existe
- coexistence preserve unmanaged
- missing messages

## Arquivos

- `crates/dare-harness/src/codex.rs`

## Validation Gates

- [ ] `cargo test -p dare-harness -- codex::`

## Definition of Done (ANTI-STUB)

- [ ] Assert 49; coexistência real
- [ ] Sem git commit
