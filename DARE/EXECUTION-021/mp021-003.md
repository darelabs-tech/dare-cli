# Task mp021-003: Manifest V2 embed + load/validate

> **Complexidade:** MED · **Depends on:** mp021-002 · **Fase:** 3  
> **BLUEPRINT-021** §4.4 §5.1 §5.9 · T-03 T-04 T-15 T-16

## Objetivo

`assets/update-manifest.v2.json` embutido carrega como `UpdateManifestV2`; validação rejeita schema≠2 e exige Codex/`AGENTS.md`.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `assets/update-manifest.v2.json` |
| MOD | `assets/manifest.yml` (se verify exigir hash do novo ficheiro) |
| CRIAR | `crates/dare-update/src/manifest_v2.rs` |
| MOD | `crates/dare-update/src/lib.rs` |

## Contratos

```rust
pub fn load_desired_manifest_v2_from_str(s: &str) -> CoreResult<UpdateManifestV2>;
pub fn load_desired_manifest_v2_embedded() -> CoreResult<UpdateManifestV2>;
```

- `schemaVersion` sempre `2`; mensagem Config: `unsupported update manifest schemaVersion`
- `DesiredAsset`: `path`, `sha256` (64 hex lower), `appliesTo` non-empty, opcional `kind`/`source`
- MUST: asset `path=="AGENTS.md"` com `appliesTo` contendo `"codex"`
- MUST: `releases` contém `0.1.0-alpha.0`; ≥1 asset `*`; cobertura harnesses
- Path: `assert_safe_asset_path` / SafeRelativePath rules
- V1: `dare_contracts::update_manifest_from_str` continua a aceitar schema 1

## Testes

- `v2_rejects_schema_1`
- `v2_requires_codex_agents`
- `v2_rejects_bad_path`
- `legacy_v1_still_loads`

## Validation Gates

- [ ] `cargo test -p dare-update`
- [ ] Embed path `update-manifest.v2.json` resolvido via `EmbeddedAssets`
- [ ] Sem `plan_update` · Sem CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Load/validate reais com erros tipados
- [ ] JSON canónico no repo (não placeholder vazio)
- [ ] Sem `todo!()` em API pública
- [ ] Sem git commit

## Segurança

- [ ] RS-08: JSON malformado → Config, sem panic
- [ ] RS-01: paths sem `..` / absolutos
