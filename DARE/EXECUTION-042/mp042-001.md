# Task mp042-001: cosine + SearchOptions + hybrid_query_with_warnings

> **Complexity:** MED · **Depends on:** — · **Rank:** 0

## Objetivo

Preparar o canal de busca para RRF 3-listas sem introduzir fastembed: cosine, flag `no_semantic`, API com warnings, paridade 041.

## Arquivos

| Ação | Caminho |
|------|---------|
| MODIFICAR | `crates/dare-graph/src/search.rs` |
| MODIFICAR | `crates/dare-graph/src/lib.rs` |

## Assinaturas (MUST)

```rust
pub fn cosine_similarity(a: &[f32], b: &[f32]) -> f64;

pub struct SearchOptions {
    pub limit: usize,
    pub max_hops: usize,
    pub fanout: usize,
    pub no_semantic: bool, // NEW; Default::default() => false
}

pub fn hybrid_query_with_warnings(
    graph: &dyn KnowledgeGraph,
    query: &str,
    opts: &SearchOptions,
) -> CoreResult<(Vec<RankedHit>, Vec<String>)>;
```

`hybrid_query` permanece público e preserva golden 041 (2-list quando sem vector).

## Edge cases

| Caso | Resultado |
|------|-----------|
| len(a)≠len(b) | cosine 0.0 |
| \|a\|=0 ou \|b\|=0 | 0.0 |
| query `""` / whitespace | `InvalidInput` `"query must not be empty"` |
| `no_semantic=true` | 2-list RRF idêntico a 041 |

## Testes esperados

- `cosine_similarity_zero_norm`
- `cosine_similarity_len_mismatch`
- `hybrid_no_semantic_matches_prior_golden` (ou assert ordem ids)
- Existentes `rrf_*` / `hybrid_*` verdes

## Fora de escopo

fastembed, CLI, docs DEC

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()` / stubs
- [ ] Cosine nunca retorna NaN (testado)
- [ ] Golden 041 não regride
- [ ] `no_semantic` coberto por teste
