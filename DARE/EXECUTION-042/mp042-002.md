# Task mp042-002: semantic.rs + fastembed + cache + ensure_model

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Feature Cargo `semantic` + módulo de modelo/cache/download confirmado + doctor types.

## Arquivos

| Ação | Caminho |
|------|---------|
| MODIFICAR | `crates/dare-graph/Cargo.toml` |
| MODIFICAR | `crates/dare-cli/Cargo.toml` |
| CRIAR | `crates/dare-graph/src/semantic.rs` |
| MODIFICAR | `crates/dare-graph/src/lib.rs` |

## Feature (MUST)

```toml
# dare-graph
[features]
default = []
semantic = ["dep:fastembed"]
```

`dare-cli`: `semantic = ["dare-graph/semantic"]`, **não** default.

## API (MUST — cfg semantic salvo doctor)

Ver BLUEPRINT-042 §0.3: `models_cache_dir`, `model_is_cached`, `ensure_model`, `embed_texts`, `SemanticDoctorReport`, `semantic_doctor()`.

Constantes §0.1; allowlist hosts §0.4.

## Confirmação download

1. Mostrar hosts allowlist + `EXPECTED_MODEL_BYTES` (~22 MB)
2. TTY: `y/N`; non-TTY: `--yes` via opts **ou** `DARE_GRAPH_SEMANTIC_YES=1`
3. User N → Ok cancel path (CLI trata exit 0) / Err tipado documentado
4. Set cache path env para fastembed; init modelo MiniLM

## Edge cases

| Caso | Resultado |
|------|-----------|
| feature off | `semantic_doctor().semantic_compiled == false` |
| path com `..` | InvalidInput |
| já cached | `ensure_model` idempotente sem re-download |

## Testes

- doctor compiled false sem feature / true com feature
- cache dir termina com `.dare/models/all-minilm-l6-v2`
- network `ensure_model` = `#[ignore]`

## Definition of Done (ANTI-STUB)

- [ ] Feature não está em `default`
- [ ] Sem schema graph.db novo
- [ ] Path jail testado
- [ ] Sem secrets/API keys
