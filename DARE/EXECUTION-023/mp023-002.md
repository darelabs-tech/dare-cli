# Task mp023-002: Tipos DesignInput/Report + render_canonical + markers

> **Complexidade:** MED · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-023** §4–5.2 · T-01 T-03 T-04 T-05 T-07 T-13

## Objetivo

Criar o módulo `design` com tipos públicos, validação de descrição, título derivado e markdown canónico com 4 markers enrichable — sem I/O de merge/CLI ainda.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-cli/src/commands/design.rs` |
| MOD | `crates/dare-cli/src/commands/mod.rs` |
| REF | `assets/templates/DESIGN-template.md` (SoT de secções) |

## Contratos

### Constantes

```rust
pub const DESIGN_REL: &str = "DARE/DESIGN.md";
pub const DESIGN_SCHEMA_VERSION: u32 = 1;
pub const DESC_MAX: usize = 32_768;
pub const DESIGN_READ_CAP: usize = 262_144;
pub const ENRICHABLE: &[&str] = &[
    "description",
    "objectives",
    "functional-requirements",
    "stack",
];
```

Markers: `<!-- AGENT:BEGIN section="<id>" -->` … `<!-- AGENT:END section="<id>" -->`.

### Tipos

- `DesignInput { title, description, interactive, fixed_date: Option<String> }`
- `DesignReport` JSON camelCase: `schemaVersion`, `mode` (`"design"`), `ok`, `path`, `action`, `title`, `markerCount`, `preservedRegions`, `interactive`, `warnings`

### Funções MUST

| Função | Comportamento |
|--------|---------------|
| `validate_description(desc)` | trim empty → InvalidInput; `len > DESC_MAX` → InvalidInput |
| `derive_title(desc)` | trim; vazio → `"Untitled"`; senão `chars().take(60)` |
| `render_canonical(input)` | 12 secções do template; 4 markers; `fixed_date` ou hoje UTC |
| `format_human(r)` | bloco §5.6 Blueprint |
| `report_to_json(r)` | Value com keys camelCase |

### Fora de escopo desta task

- `merge_preserve`, `apply_design`, clap `Commands::Design`, flag `--ai`

## Testes MUST

| Teste | Assert |
|-------|--------|
| `validate_description_rejects_empty` | Err |
| `validate_description_rejects_oversize` | Err |
| `derive_title_truncates_60` | len ≤ 60 |
| `render_contains_four_enrichable_markers` | 4 BEGIN + 4 END |
| `render_stable_with_fixed_date` | duas chamadas iguais com `fixed_date=Some("1970-01-01")` |
| `report_schema_version_1` | JSON `schemaVersion==1`, `mode=="design"` |

## Validation Gates

- [ ] `cargo test -p dare-cli -- design`
- [ ] Sem merge/CLI · Sem `--ai` · Sem git commit

## Segurança

- RS-01: caps `DESC_MAX`
- RS-07: markers só em HTML comments

## Definition of Done (ANTI-STUB)

- [ ] Nenhum `todo!()` / `unimplemented!()` em paths públicos
- [ ] `render_canonical` produz markdown real (não string vazia / stub)
- [ ] Sem git commit
