# Task mp046-003: AX generators

> **Complexity:** MED · **Depends on:** mp046-001 · **Rank:** 1

## Objetivo

Gerar os 7 artefatos AX por stack (BLUEPRINT §0.3).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-scaffold/src/ax.rs` |
| MOD | `lib.rs` exports |
| OPCIONAL | templates AX sob `assets/stacks/<id>/ax/` |

## MUST — 7 paths

1. `llms.txt`
2. `README.md` com `## Bootstrap` e `## Docs`
3. `.env.example` sem secrets
4. `openapi.json` — backend: path `/healthz`; MCP: `"paths":{}`
5. `Dockerfile`
6. `docker-compose.yml`
7. rate-limit em `metadata.rate_limit_rel`

## Testes

- `ax_paths_for_each_stack`
- `openapi_mcp_stub_empty_paths`
- `readme_has_required_sections`

## Fora de escopo

templates skeleton (002), apply (004), CLI.

## Definition of Done (ANTI-STUB)

- [ ] 11 stacks × 7 paths cobertos por teste
- [ ] Secret scan nos AX gerados
- [ ] Sem `todo!()`
