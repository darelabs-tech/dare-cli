# Task mp046-004: plan / apply / rollback

> **Complexity:** HIGH · **Depends on:** mp046-002, mp046-003 · **Rank:** 2

## Objetivo

Implementar `plan_scaffold`, `apply_scaffold`, `run_scaffold` com journal/rollback (BLUEPRINT §0.5).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-scaffold/src/plan.rs` |
| CRIAR | `crates/dare-scaffold/src/apply.rs` |
| MOD | trait impls / `lib.rs` |

## MUST

- `project_name` regex `^[a-z][a-z0-9_-]{0,63}$`
- `frontend: Some(_)` → InvalidInput `frontend composition reserved for 047`
- `!force` + path existe → InvalidInput `path already exists: …`
- `force` → `Replace`; senão `Create`
- Items sort path ASC; kinds `template|ax|meta`
- Journal `.dare/scaffold-session-*`; rollback em falha; sucesso apaga journal
- `check=true` → zero writes; report `check=true`
- Só FS + `SafeRelativePath` (sem shell)

## Testes

- `plan_sorted`
- `check_zero_write`
- `force_replace`
- `rollback_restores` (falha injectada)

## Definition of Done (ANTI-STUB)

- [ ] Rollback com assert FS restaurado
- [ ] Check mode sem mutação
- [ ] Nenhum stub vazio nas APIs públicas
