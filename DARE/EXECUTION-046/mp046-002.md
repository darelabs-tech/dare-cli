# Task mp046-002: templates + render

> **Complexity:** HIGH · **Depends on:** mp046-001 · **Rank:** 1

## Objetivo

Portar templates MVP `assets/stacks/<id>/` + `render.rs` com secret scan (BLUEPRINT §0.6).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `assets/stacks/<id>/**` (11 stacks) |
| CRIAR | `crates/dare-scaffold/src/render.rs` |
| MOD | `assets/manifest.yml` (hashes se verify exigir) |
| MOD | `crates/dare-scaffold/src/lib.rs` |

## MUST

- Cada stack: `dare.config.json.tpl` + skeleton §0.6
- Replace só `{{project_name}}` e `{{stack_id}}`
- Needles: `password=`, `api_key=`, `BEGIN PRIVATE KEY` → InvalidInput `template contains forbidden secret pattern`
- Embed/verify `dare-assets` continua verde

## Testes

- `render_substitutes`
- `secret_scan_rejects_api_key`
- `each_stack_template_root_exists` (ou equivalente)

## Fora de escopo

AX generators (003), apply (004), CLI.

## Definition of Done (ANTI-STUB)

- [ ] 11 roots existem com conteúdo não-vazio
- [ ] Secret scan com assert real
- [ ] `cargo test -p dare-assets` ok
