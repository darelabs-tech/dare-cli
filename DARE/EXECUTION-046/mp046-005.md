# Task mp046-005: validate + fixtures greenfield

> **Complexity:** MED · **Depends on:** mp046-004 · **Rank:** 3

## Objetivo

`validate_stack_output` + integration fixtures para 3 famílias (BLUEPRINT Fase E).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-scaffold/src/validate.rs` |
| CRIAR | `crates/dare-scaffold/tests/greenfield_fixtures.rs` |
| MOD | `lib.rs` |

## MUST

- Validate: 7 AX + `dare.config.json`; `missing` ASC; `secret_hits`; `ok = missing.is_empty() && secret_hits.is_empty()`
- Fixtures: `run_scaffold` happy para `node-nestjs`, `rust-axum`, `mcp-node-ts` + validate `ok=true`
- Unknown stack → InvalidInput

## Testes

- `validate_missing_lists`
- `validate_unknown_stack`
- Integration em `greenfield_fixtures`

## Definition of Done (ANTI-STUB)

- [ ] 3 stacks integration passam
- [ ] Validate não é no-op
