# Task mp046-001: crate + types + registry

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Criar `crates/dare-scaffold` no workspace com types + registry das 11 stacks (BLUEPRINT-046 §0).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-scaffold/Cargo.toml` |
| CRIAR | `crates/dare-scaffold/src/lib.rs` |
| CRIAR | `crates/dare-scaffold/src/types.rs` |
| CRIAR | `crates/dare-scaffold/src/registry.rs` |
| CRIAR | `crates/dare-scaffold/src/trait_api.rs` |
| MOD | `Cargo.toml` (workspace members) |

## MUST

- `list_stack_ids()` → 11 ids ASC exactos §0.2
- `scaffolder_for`: unknown → InvalidInput contendo `unknown stack id`
- `rails` → mensagem contém `did you mean ruby-rails-8?`
- Metadata completa incl. `rate_limit_rel` por família §0.3
- Trait `StackScaffolder`; table-driven `GenericScaffolder` ok
- `plan()` neste rank: `Ok(ScaffoldPlan { items: [], … })` temporário aceite
- Sem CLI; deps: `dare-core`, `dare-contracts`, `dare-assets`, serde

## Testes

- `registry_lists_eleven_sorted`
- `scaffolder_unknown`
- `scaffolder_rails_hint`
- `metadata_rate_limit_paths`

## Validation Gates

- [ ] `cargo test -p dare-scaffold -- registry_`
- [ ] `cargo clippy -p dare-scaffold --all-targets -- -D warnings`

## Definition of Done (ANTI-STUB)

- [ ] Nenhum `todo!()` / `unimplemented!()` em exports públicos (plan vazio ok)
- [ ] 11 metadatas reais
- [ ] Asserts reais nos 4 testes
