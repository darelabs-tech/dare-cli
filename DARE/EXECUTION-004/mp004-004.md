# TASK mp004-004: ExecutionContext + telemetry + uuid

> **Complexidade:** MED | **Depends on:** mp004-002 | **Estimativa:** 1 h  
> **Microplano:** 004 | **Fase Blueprint:** 4

## 1. OBJETIVO

`ExecutionContext` + `ColorMode` + `init_tracing`; correlation_id UUID v4; pin `uuid 1.16.0`.

## 2. ARQUIVOS

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-core/src/context.rs` |
| CRIAR/MOVER | `crates/dare-core/src/telemetry.rs` |
| EDIT | `Cargo.toml` workspace + dare-core |
| EDIT/REMOVER | `tracing_init.rs` (migrar) |

## 3. IMPLEMENTAÇÃO

§4.3 / §5.2 Blueprint:
- `from_cli(json, no_color)` + `NO_COLOR`
- `init_tracing` default filter `warn`
- `init_test_subscriber` preservado

### Testes
- [ ] NO_COLOR ⇒ Never
- [ ] correlation_id formato UUID

## 4. SEGURANÇA

- [ ] RS-07: UUID random, não PII

## 5. VALIDATION GATES

```bash
cargo test -p dare-core
```

## 6. Definition of Done (ANTI-STUB)

- [ ] Dep uuid pinada
- [ ] TASKS-004 → mp004-004 DONE

## 7. PRÓXIMA

`mp004-005`
