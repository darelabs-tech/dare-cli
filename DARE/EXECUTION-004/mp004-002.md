# TASK mp004-002: ErrorKind + CoreError + exit_code

> **Complexidade:** MED | **Depends on:** — | **Estimativa:** 1 h  
> **Microplano:** 004 | **Fase Blueprint:** 2

## 1. OBJETIVO

`ErrorKind` + `exit_code` + `CoreError` tipados; `InvalidArgument` removido; testes de mapping estáveis.

## 2. ARQUIVOS

| Ação | Caminho |
|------|---------|
| REWRITE | `crates/dare-core/src/error.rs` |
| EDIT | `crates/dare-core/src/lib.rs` |
| EDIT | call sites `InvalidArgument` → `InvalidInput` |

## 3. IMPLEMENTAÇÃO

Conforme BLUEPRINT-004 §4.1–4.2:

| Kind | Exit |
|------|------|
| Internal | 1 |
| Usage | 2 |
| NotFound | 3 |
| InvalidInput, Config | 4 |
| Io | 5 |

`validate_nonempty_name` → `CoreError::InvalidInput`.

### Testes
- [ ] `exit_code_mapping_is_stable`
- [ ] testes existentes de validate_* atualizados

## 4. SEGURANÇA

- [ ] Sem `anyhow` em dare-core
- [ ] Sem `unwrap()` em API pública nova

## 5. VALIDATION GATES

```bash
cargo test -p dare-core
```

## 6. Definition of Done (ANTI-STUB)

- [ ] Nenhum `InvalidArgument` no repo
- [ ] Mapping exhaustivo + testes
- [ ] TASKS-004 → mp004-002 DONE

## 7. PRÓXIMA

`mp004-004`, `mp004-005`
