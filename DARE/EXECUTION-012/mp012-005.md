# Task mp012-005: CLI smoke + docs DEC-013 + exceptions

## Objetivo

Fase 5 — smoke + documentação; rules deferred.

## Smoke

1. tempdir `--root`
2. `dare harness cursor install --force --root <tmp>` → `wrote 49`
3. `validate` → `ok` + `49`
4. `detect` → `cursor_dir=true` `cursorrules=true`

## Docs

Expandir `docs/compatibility/harness-cursor.md`:

- CLI, preserve, T-01…T-14, RS
- SoT 49 vs baseline 33/25 + exceptions Classe C
- **Deferred:** rules `.mdc` / `install_cursor_rules`

Verificar exceptions no YAML embutido. **Não** remover. **Não** implementar rules.

## Arquivos

- `crates/dare-cli/tests/cli_smoke.rs`
- `docs/compatibility/harness-cursor.md`

## Validation Gates

- [ ] `cargo test -p dare-cli --test cli_smoke`

## Definition of Done (ANTI-STUB)

- [ ] Smoke real
- [ ] Docs DEC-013 + rules deferred
- [ ] Sem git commit
