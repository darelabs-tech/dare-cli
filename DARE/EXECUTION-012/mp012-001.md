# Task mp012-001: Verificar docker-compose.ci.yml

## Objetivo

Fase 1 — confirmar containerização CI parseável.

## Passos

1. `docker compose -f docker-compose.ci.yml config`
2. Se Docker ausente: documentar YAML parseável no `--output`

## Definition of Done (ANTI-STUB)

- [ ] `config` exit 0 **ou** YAML documentado
- [ ] Sem git commit
