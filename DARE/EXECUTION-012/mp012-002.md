# Task mp012-002: Congelar detect + generate_cursorrules + preserve

## Objetivo

Congelar BLUEPRINT-012 §5.1–5.2 em `dare-harness::cursor`.

## Assinaturas

```rust
pub struct CursorDetect { pub cursor_dir: bool, pub cursorrules: bool }
pub fn detect_cursor(root: &ProjectRoot) -> CoreResult<CursorDetect>;
pub fn generate_cursorrules(root: &ProjectRoot, force: bool) -> CoreResult<()>;
```

## Regras

1. `detect_cursor` — zero writes
2. `generate_cursorrules` — prefixo `<!-- dare:managed`; body §5.2
3. Preserve: unmanaged + `!force` → no-op

## Testes

- detect empty → ambos false
- generate → managed
- preserve unmanaged `.cursorrules`

## Arquivos

- `crates/dare-harness/src/cursor.rs`

## Validation Gates

- [ ] `cargo test -p dare-harness -- cursor::`

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()`; sem `install_cursor_rules`
- [ ] Sem git commit
