# Task mp012-003: install_cursor_commands + validate

## Objetivo

Fase 3 — 49 commands da matrix; preserve unmanaged.

## Assinaturas

```rust
pub fn install_cursor_commands(root: &ProjectRoot, force: bool) -> CoreResult<usize>;
pub fn validate_cursor_install(root: &ProjectRoot) -> CoreResult<usize>;
```

## Regras

- Body: `<!-- dare:managed capability={id} -->` + `render_claude_command`
- force → 49; unmanaged skip; validate existência; missing amostra ≤5
- **Não** instalar `.mdc`

## Testes

- `install_validate_force` → 49/49
- preserve unmanaged command → 48
- validate missing message

## Arquivos

- `crates/dare-harness/src/cursor.rs`

## Validation Gates

- [ ] `cargo test -p dare-harness -- cursor::`

## Definition of Done (ANTI-STUB)

- [ ] Assert 49 no force path
- [ ] Sem git commit
