# Task mp012-004: CLI help `--force` + install pipeline

## Objetivo

Fase 4 — help force + ordem install.

## Alterações

- `CursorCmd::Install`: docstring `--force` — unmanaged files overwritten when set
- Ordem: `generate_cursorrules` → `install_cursor_commands` (sem rules)

## Arquivos

- `crates/dare-cli/src/main.rs`

## Validation Gates

- [ ] `cargo build -p dare-cli`

## Definition of Done (ANTI-STUB)

- [ ] Help `--force` explícito
- [ ] Sem git commit
