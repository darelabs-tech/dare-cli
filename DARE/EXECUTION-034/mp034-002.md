# Spec: mp034-002 — Unicode layer

## Goal
Create `crates/dare-guard` with unicode detection (ZW, bidi, VS, tags, homoglyphs) and modes strip|block.

## Acceptance
- Corpus detects malicious unicode
- strip removes; block yields FAIL findings
