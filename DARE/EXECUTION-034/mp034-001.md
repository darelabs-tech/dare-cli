# Spec: mp034-001 — ErrorKind GuardFail

## Goal
Add `ErrorKind::GuardFail` / `CoreError::GuardFail` mapped to exit code **6**.

## Acceptance
- `exit_code(GuardFail) == 6`
- `as_str() == "GuardFail"`
- Existing 1–5 mapping unchanged
