# Spec: mp034-008 — Agent preflight

## Goal
Replace `guard_preflight_stub` with `dare_guard::run_preflight`; FAIL blocks agent (exit 6).

## Acceptance
- Malicious DARE artifact → agent does not start
- Clean project → agent proceeds
