# Spec: mp034-003 — Injection scan

## Goal
Ship `assets/rules/scan-rules.json` (4 rules) and apply regex scan with redacted evidence.

## Acceptance
- Load rules; override via `DARE_GUARD_SCAN_RULES_PATH`
- Matches redact secrets in evidence snippets
