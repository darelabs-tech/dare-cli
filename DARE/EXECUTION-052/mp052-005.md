# Task mp052-005: streamable-http serve (port 3100)

> **Complexity:** HIGH · **Depends on:** mp052-003 · **Rank:** 2

## Objetivo

Ao final, MCP streamable-http escuta em `127.0.0.1:3100` com auth loopback/Bearer alinhada ao 051.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR/MOD | `crates/dare-server/src/mcp/*` (`serve_streamable_http`) |
| MOD | `commands/mcp.rs` (flags `--bind`/`--port` quando transport http) |

## MUST

```rust
pub async fn serve_streamable_http(
    ctx: ServiceCtx, bind: IpAddr, port: u16, token: Arc<str>, force_auth: bool,
) -> CoreResult<()>;
```

- Defaults: bind `127.0.0.1`, port `3100`
- Env: `DARE_MCP_HTTP_BIND`, `DARE_MCP_HTTP_PORT`
- force_auth / non-loopback → 401
- Graceful shutdown

## Testes

- `http_force_auth_401` (ou Class B documentado se limitação rmcp)

## Definition of Done (ANTI-STUB)

- [ ] Serve HTTP real com mesmo handler de tools
- [ ] Não colidir com REST :3000
