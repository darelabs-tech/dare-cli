# Task mp052-006: Alias bin dare-mcp-server (REST + deprecation)

> **Complexity:** MED · **Depends on:** — · **Rank:** 0

## Objetivo

Ao final, o binário `dare-mcp-server` sobe **REST** e imprime deprecation — nunca MCP.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-cli/Cargo.toml` (`[[bin]]`) |
| CRIAR | `crates/dare-cli/src/bin/dare_mcp_server.rs` |
| CRIAR | teste smoke (em `tests/` ou unit) |

## MUST

```text
MSG_ALIAS_DEPRECATED = "dare-mcp-server is deprecated: it serves legacy REST only. Use 'dare server --protocol rest' or 'dare mcp serve' for MCP."
```

- stderr sempre
- Depois: `AppMode::Rest` / mesma lógica `commands/server.rs`
- Args: `--bind`, `--port`, `-d`

## Definition of Done (ANTI-STUB)

- [ ] Zero JSON-RPC no alias
- [ ] Teste stderr contains deprecated
