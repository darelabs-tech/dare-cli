# Task mp052-001: Services layer + REST delegation

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Ao final, `services/*` concentra o domínio e as rotas REST 051 apenas delegam, com suite HTTP intacta.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-server/src/services/{mod,project,graph,dag,task,steering}.rs` |
| MOD | `crates/dare-server/src/routes/{project,context,blueprint,dag,tasks,graph,steering}.rs` |
| MOD | `crates/dare-server/src/lib.rs` |

## MUST

```rust
pub struct ServiceCtx { pub root: ProjectRoot }
pub fn project_snapshot(ctx: &ServiceCtx) -> CoreResult<ProjectSnapshot>;
pub fn read_blueprint(ctx: &ServiceCtx) -> CoreResult<BlueprintDoc>;
pub fn context_query(ctx: &ServiceCtx, kind: &str, query: &str) -> CoreResult<ContextQueryResponse>;
pub fn dag_load_json(ctx: &ServiceCtx) -> CoreResult<serde_json::Value>;
pub fn task_get/put(...);
pub fn graph_locate/traverse/map_requirement(...);
pub fn steering_show(ctx: &ServiceCtx, file: &str) -> CoreResult<SteeringShowReport>;
```

- Wire JSON HTTP Class A (mesmo status/body)
- Sem `rmcp`

## Testes

- services unit + regression `cargo test -p dare-server`

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()`; handlers sem lógica de negócio duplicada
- [ ] Suite 051 verde
