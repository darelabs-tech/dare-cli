# Task mp052-002: Workspace rmcp pins + feature mcp scaffold

> **Complexity:** MED · **Depends on:** — · **Rank:** 0

## Objetivo

Ao final, `rmcp =3.0.1` está pinned e `dare-server` compila com feature `mcp` (default-on) e sem ela.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `Cargo.toml` (workspace deps) |
| MOD | `crates/dare-server/Cargo.toml` |
| CRIAR | `crates/dare-server/src/mcp/mod.rs` (stub cfg) |
| MOD | `crates/dare-server/src/lib.rs` |

## MUST

- Features: `server`, `macros`, `transport-io`, `transport-streamable-http-server`, `schemars`
- `default = ["mcp"]`
- Stub: `pub fn mcp_feature_ok() -> bool { true }` sob `cfg(feature = "mcp")`
- Não implementar tools (003)

## Validation Gates

```bash
cargo check -p dare-server --all-features
cargo check -p dare-server --no-default-features
```

## Definition of Done (ANTI-STUB)

- [ ] Pins `=` exactos
- [ ] check all-features e no-default-features OK
