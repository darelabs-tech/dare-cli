# Task mp026-003: TaskStatus + cascading skip

> **Complexidade:** MED · **Depends on:** mp026-002 · **Fase:** 3  
> **BLUEPRINT-026** §5.6–5.7 · T-09 T-10 T-11

## Objetivo

Status canónicos + cascading skip fixpoint (só PENDING).

## Arquivos

| Ação | Path |
|------|------|
| CRIAR | `crates/dare-dag/src/status.rs` |
| CRIAR/MOD | `crates/dare-dag/src/state.rs` — `apply_cascading_skip` |
| MOD | `lib.rs` exports |
| CRIAR | `tests/fixtures/dag/skip-cascade.v21.yaml` |

## Assinaturas MUST

```rust
pub enum TaskStatus { Pending, Running, Done, Failed, Skipped }
impl TaskStatus {
    pub fn as_str(&self) -> &'static str; // PENDING|…
    pub fn parse(s: &str) -> CoreResult<Self>;
}

pub fn apply_cascading_skip(state: &mut RuntimeStateV1, doc: &DagDocument) -> usize;
```

## Regras

- Só `PENDING` → `SKIPPED` se alguma dep ∈ `{FAILED,SKIPPED}`
- `RUNNING` nunca auto-skipped
- Idempotente (2ª chamada → 0)
- Dep ausente no state ≠ FAILED/SKIPPED

## Validation Gates

- [x] Testes skip_* + task_status_roundtrip
- [x] `cargo test -p dare-dag -- skip`
- [x] Sem FileLock/transition · Sem git commit

## Definition of Done (ANTI-STUB)

- [x] Sem stubs · Fixpoint termina · RUNNING preservado em teste explícito

## Próxima Task

mp026-004 · mp026-006 (paralela possível)
