# Task mp026-006: next_executable + ranks_validated + proptest

> **Complexidade:** MED · **Depends on:** mp026-002, mp026-003 · **Fase:** 6  
> **BLUEPRINT-026** §5.3–5.4 · T-21 T-23 · RF-20

## Objetivo

Helpers para 027/028 + property tests de ranks/skip.

## Arquivos

| Ação | Path |
|------|------|
| MOD | `crates/dare-dag/src/graph.rs` |
| MOD | root `Cargo.toml` — `proptest = "=1.36.0"` |
| MOD | `crates/dare-dag/Cargo.toml` — dev-dep proptest |

## Assinaturas

```rust
pub fn compute_ranks_validated(
    doc: &DagDocument,
    opts: &ValidateOptions,
    ctx: &ValidateFsContext<'_>,
) -> Result<BTreeMap<String, u32>, DagGraphError>;

pub fn next_executable(
    doc: &DagDocument,
    state: &RuntimeStateV1,
    ranks: &BTreeMap<String, u32>,
) -> Vec<String>;
```

## Regras next_executable

PENDING ∧ ∀ deps status==DONE; ordem: rank↑, id lexico.

## Property tests

1. `prop_ranks_gt_deps` — DAG acíclico: rank(t) > rank(d) e ≥0  
2. `prop_skip_idempotent` — 2ª `apply_cascading_skip` → 0

## Validation Gates

- [x] Units + props verdes
- [x] `cargo test -p dare-dag`
- [x] Sem git commit

## Definition of Done (ANTI-STUB)

- [x] Props reais (não `proptest(true)`) · Sem stubs

## Próxima Task

mp026-007
