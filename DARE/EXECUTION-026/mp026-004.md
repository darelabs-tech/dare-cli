# Task mp026-004: State store ensure/transition + FileLock

> **Complexidade:** HIGH · **Depends on:** mp026-003 · **Fase:** 4  
> **BLUEPRINT-026** §5.8–5.9 · T-03 T-04 T-12–T-16 T-19 T-20

## Objetivo

Persistir `.dare/state.json` com lock fail-fast e transições atómicas.

## Arquivos

| Ação | Path |
|------|------|
| COMPLETAR | `crates/dare-dag/src/state.rs` |
| MOD | `lib.rs` |

## Constantes / tipos

```rust
pub const STATE_REL: &str = ".dare/state.json";

pub trait Clock { fn now_rfc3339(&self) -> String; }
pub struct SystemClock;
pub struct FixedClock(pub String);

pub enum Transition {
    Start,
    Complete { output: String },
    Fail { error: String },
    Reset,
    Skip,
}
pub enum RefreshCanvas { Yes, No }

pub fn ensure_state(root: &ProjectRoot, doc: &DagDocument, clock: &dyn Clock) -> CoreResult<RuntimeStateV1>;
pub fn transition(
    root: &ProjectRoot,
    doc: &DagDocument,
    task_id: &str,
    tr: Transition,
    clock: &dyn Clock,
    refresh: RefreshCanvas,
) -> CoreResult<RuntimeStateV1>;
```

## Comportamento

1. `FileLock::try_acquire(root, STATE_REL)` — held → `io("file lock held")`
2. Load ou init v1; merge tasks do DAG como PENDING + dependsOn
3. Matriz §5.6; `AttemptRecord` em Complete/Fail; Reset mantém attempts, zera output/error
4. `apply_cascading_skip`; `updated_at`; `save_runtime_state`
5. Nesta task: testes com `RefreshCanvas::No` (Yes em mp026-005)
6. Orphans fora do DAG: preservar; tasks novas: inserir PENDING

## Edge cases

| Caso | Resultado |
|------|-----------|
| task_id ∉ DAG | NotFound |
| Start em DONE | invalid_input |
| version≠1 | Config |
| lock held | Io |

## Validation Gates

- [x] Testes ensure/transition/lock/version
- [x] `cargo test -p dare-dag -- state`
- [x] Sem git commit · Sem CLI execute

## Definition of Done (ANTI-STUB)

- [x] Só `save_runtime_state` / atomic · Sem write parcial
- [x] Lock test real (hold + second fail)

## Próxima Task

mp026-005
