# Task mp026-002: graph ranks + find_cycle_path + fixtures

> **Complexidade:** HIGH · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-026** §5.1–5.5 · T-05 T-06 T-07 T-28

## Objetivo

API de ranks longest-path (roots=0) e detecção de ciclo reutilizada do validate 020.

## Arquivos a Criar/Modificar

| Ação | Path |
|------|------|
| CRIAR | `crates/dare-dag/src/graph.rs` |
| MODIFICAR | `crates/dare-dag/src/lib.rs` — `mod graph; pub use …` |
| MODIFICAR | `crates/dare-dag/src/validate.rs` — `find_cycle_path` |
| CRIAR | `tests/fixtures/dag/ranks-chain.v21.yaml` + `.ranks.json` |
| CRIAR | `tests/fixtures/dag/ranks-diamond.v21.yaml` + `.ranks.json` |
| CRIAR | `tests/fixtures/dag/ranks-fanout.v21.yaml` + `.ranks.json` |

## Assinaturas MUST

```rust
pub struct TaskView { pub id: String, pub title: String, pub depends_on: Vec<String> }
pub fn iter_task_views(doc: &DagDocument) -> Vec<TaskView>;

pub enum DagGraphError {
    Cycle { path: Vec<String> },
    MissingDependency { task_id: String, missing: String },
    InvalidDag { message: String },
}

pub fn compute_ranks(doc: &DagDocument) -> Result<BTreeMap<String, u32>, DagGraphError>;
pub fn tasks_by_rank(ranks: &BTreeMap<String, u32>) -> BTreeMap<u32, Vec<String>>;
pub(crate) fn find_cycle_path(doc: &DagDocument) -> Option<Vec<String>>;
```

## Algoritmo

`rank(t)=0` se deps vazio; senão `1+max(rank(deps))`. Ciclo → `Cycle` (path canónico).

## Validation Gates

- [x] Goldens chain/diamond/fanout passam
- [x] Ciclo → erro tipado
- [x] `cargo test -p dare-dag -- graph`
- [x] Sem state/canvas/CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [x] Sem `todo!`/`unimplemented!` · Sem funções públicas vazias
- [x] Mocks só em testes · Edge cases: empty DAG, missing dep, cycle

## Testes esperados

- `compute_ranks_chain` / `_diamond` / `_fanout`
- `compute_ranks_cycle_errors`
- `tasks_by_rank_sorted`
- `iter_task_views_legacy`

## Próxima Task

mp026-003 (skip) · mp026-006 (após 002+003)
