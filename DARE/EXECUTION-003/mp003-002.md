# TASK mp003-002: deny.toml + cargo deny check

> **Complexidade:** MED | **Depends on:** — | **Estimativa:** 0,75 h  
> **Microplano:** 003 | **Fase Blueprint:** 2

## 1. OBJETIVO

`deny.toml` versionado; `cargo deny check` exit 0 com política do Blueprint §4.2.

## 2. CONTEXTO

- Pins: `cargo-deny` **0.18.2**
- Exceptions de license só se documentadas; nunca silenciar advisories

## 3. ARQUIVOS

| Ação | Caminho |
|------|---------|
| CRIAR | `deny.toml` |

## 4. IMPLEMENTAÇÃO

Conteúdo canônico BLUEPRINT-003 §4.2 (`advisories` / `licenses` allowlist / `bans` / `sources`).

```bash
cargo install cargo-deny --version 0.18.2 --locked
cargo deny check
```

### Edge cases
| Caso | Ação |
|------|------|
| License transitiva fora da allowlist | Exception nomeada + comentário + nota DEC se necessário |
| Advisory vulnerability | Falhar — bump dep (não ignore) |

## 5. SEGURANÇA

- [ ] RS-04: deny policies ativas
- [ ] Sem `advisories.ignore` em massa

## 6. VALIDATION GATES

```bash
cargo deny check
```

## 7. Definition of Done (ANTI-STUB)

- [ ] `deny.toml` completo (não stub vazio)
- [ ] `cargo deny check` exit 0
- [ ] TASKS-003 → mp003-002 DONE

## 8. PRÓXIMA

`mp003-004` (ci.yml)
