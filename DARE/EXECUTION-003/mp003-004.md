# TASK mp003-004: Workflow ci.yml

> **Complexidade:** HIGH | **Depends on:** mp003-002 | **Estimativa:** 1 h  
> **Microplano:** 003 | **Fase Blueprint:** 3

## 1. OBJETIVO

`.github/workflows/ci.yml` com gates fmt → clippy → test → audit → deny; `permissions.contents: read`.

## 2. CONTEXTO

- Toolchain **1.85.0**; cache `Swatinem/rust-cache@v2.7.8`
- `cargo-audit` **0.21.2**; `cargo-deny` **0.18.2**
- Não tocar `governance-001.yml`

## 3. ARQUIVOS

| Ação | Caminho |
|------|---------|
| CRIAR | `.github/workflows/ci.yml` |

## 4. IMPLEMENTAÇÃO

Triggers, path filters e steps **exatos** BLUEPRINT-003 §5.1 (ordem obrigatória).

### Edge cases
- Advisory novo → job vermelho até bump
- Cache miss → gates ainda rodam

## 5. SEGURANÇA

- [ ] RS-01: paths fixos no YAML
- [ ] RS-03: `contents: read`
- [ ] RS-05: nenhum secret hardcoded

## 6. VALIDATION GATES

```bash
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo audit
cargo deny check
```

## 7. Definition of Done (ANTI-STUB)

- [ ] Todos os 10 steps presentes com pins de versão
- [ ] Nenhum step `continue-on-error: true` nos gates
- [ ] TASKS-003 → mp003-004 DONE

## 8. PRÓXIMA

`mp003-006`, `mp003-007`
