# TASK mp003-001: docker-compose.ci.yml (Fase 1)

> **Complexidade:** LOW | **Depends on:** — | **Estimativa:** 0,3 h  
> **Microplano:** 003 | **Fase Blueprint:** 1

## 1. OBJETIVO

`docker-compose.ci.yml` existe e `docker compose -f docker-compose.ci.yml config` exit 0.

## 2. CONTEXTO

- BLUEPRINT-003 §5.4
- Reusa `Dockerfile.rust` do microplano 002

## 3. ARQUIVOS

| Ação | Caminho |
|------|---------|
| CRIAR | `docker-compose.ci.yml` |

## 4. IMPLEMENTAÇÃO

Serviço `dare-ci-smoke`: build com `dockerfile: Dockerfile.rust`, `command: ["--version"]`, healthcheck `["CMD", "dare", "--version"]` (interval 30s, timeout 10s, retries 3).

### Testes
- [ ] `docker compose -f docker-compose.ci.yml config` exit 0
- [ ] Healthcheck não é `CMD true`

## 5. SEGURANÇA

- [ ] Sem secrets no compose
- [ ] Sem montar `.env` com credenciais

## 6. VALIDATION GATES

```bash
docker compose -f docker-compose.ci.yml config
```

## 7. Definition of Done (ANTI-STUB)

- [ ] Ficheiro completo (não placeholder)
- [ ] Nenhum `TODO`/`FIXME`
- [ ] `DARE/TASKS-003-ci-cross-platform-e-qualidade.md` → mp003-001 ✅ DONE

## 8. PRÓXIMA

Fecha com `mp003-008` (após docs + audit).
