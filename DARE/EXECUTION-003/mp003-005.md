# TASK mp003-005: Workflow build.yml (matrix 5)

> **Complexidade:** HIGH | **Depends on:** mp003-003 | **Estimativa:** 1,5 h  
> **Microplano:** 003 | **Fase Blueprint:** 4–5 (checksums no mesmo workflow)

## 1. OBJETIVO

`build.yml` produz binários release nos 5 targets, smoke nativo, `dare.sha256`, upload-artifact 7 dias.

## 2. CONTEXTO

| target | runner | artifact |
|--------|--------|----------|
| x86_64-unknown-linux-gnu | ubuntu-latest | dare-x86_64-unknown-linux-gnu |
| aarch64-unknown-linux-gnu | ubuntu-24.04-arm | dare-aarch64-unknown-linux-gnu |
| x86_64-apple-darwin | macos-13 | dare-x86_64-apple-darwin |
| aarch64-apple-darwin | macos-14 | dare-aarch64-apple-darwin |
| x86_64-pc-windows-msvc | windows-latest | dare-x86_64-pc-windows-msvc |

## 3. ARQUIVOS

| Ação | Caminho |
|------|---------|
| CRIAR | `.github/workflows/build.yml` |

## 4. IMPLEMENTAÇÃO

BLUEPRINT-003 §5.2 + T-07:
- `fail-fast: false`
- build `--release -p dare-cli --target`
- smoke: `scripts/ci/smoke-dare.sh` / `.ps1`
- checksum → `dare.sha256`
- `actions/upload-artifact@v4.6.0`, `retention-days: 7`
- `permissions: contents: read` + `actions: write`
- triggers: PR/push paths (+ `scripts/ci/**`, `build.yml`) + `workflow_dispatch`
- Sem GitHub Releases; sem SBOM

### Edge cases
| Caso | Tratamento |
|------|------------|
| Windows path | `dare.exe` + pwsh script |
| Runner ARM Linux | `ubuntu-24.04-arm` nativo |

## 5. SEGURANÇA

- [ ] RS-06: smoke argv direto
- [ ] RS-07: checksum anexado
- [ ] Sem secrets em artifacts

## 6. VALIDATION GATES

```bash
# Local (1 target):
cargo build --release -p dare-cli
bash scripts/ci/smoke-dare.sh target/release/dare
# Review YAML matrix completa (5 include)
```

## 7. Definition of Done (ANTI-STUB)

- [ ] Matrix com 5 células exatas
- [ ] Smoke + sha256 + upload em cada célula
- [ ] TASKS-003 → mp003-005 DONE

## 8. PRÓXIMA

`mp003-006`, `mp003-007`
