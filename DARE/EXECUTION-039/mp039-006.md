# Task mp039-006: Smokes migrate_* + Ralph close

> **Complexity:** MED · **Depends on:** mp039-005 · **Rank:** 4

## Objetivo

Cobrir smokes CLI e fechar o microplano com Ralph Loop completo.

## Arquivos

| Ação | Caminho |
|------|---------|
| MODIFICAR | `crates/dare-cli/tests/cli_smoke.rs` |
| MODIFICAR | `DARE/TASKS-039-migrate.md` (marcar DONE ao passar) |

## Smokes (nomes MUST)

| Teste | Comportamento |
|-------|----------------|
| `migrate_help_requires_to` | help/usage exige `--to` |
| `migrate_write_success` | tempdir: `Cargo.toml`+`src/lib.rs` + `DARE/IDEIA.md` + `REVERSE/reverse-facts.json` (1 módulo) → exit 0 + ficheiros sob `DARE/MIGRATION` |
| `migrate_check_no_write` | `--check` → exit 0 e zero ficheiros novos em `MIGRATION` |
| `migrate_bad_target_exit_4` | `--to Not-A-Stack` → exit 4 |
| `migrate_missing_reverse_exit_4` | sem IDEIA/modules → exit 4 |

## Ralph Loop (MUST passar)

```bash
cargo fmt --check
cargo clippy -p dare-project -p dare-cli --all-targets -- -D warnings
cargo test -p dare-project -- migrate
cargo test -p dare-cli --test cli_smoke -- migrate_
cargo audit
```

## Validation Gates

- [ ] Todos os smokes verdes
- [ ] Clippy `-D warnings`
- [ ] Audit sem HIGH/CRITICAL **novo**
- [ ] TASKS-039 6/6 DONE

## Definition of Done (ANTI-STUB)

- [ ] Smokes com asserts reais (exit code + filesystem), não `assert!(true)`
- [ ] Sem skips permanentes dos testes migrate
- [ ] Nenhum stub deixado nas tasks anteriores
