# Task mp039-003: render + run_migrate check/write

> **Complexity:** HIGH · **Depends on:** mp039-001 · **Rank:** 1

## Objetivo

Completar I/O determinístico de migrate: render + `run_migrate` com `--check` zero-write e write atómico sob `DARE/MIGRATION/**`.

## Arquivos

| Ação | Caminho |
|------|---------|
| MODIFICAR | `crates/dare-project/src/migrate.rs` |

## Assinaturas (MUST)

```rust
pub fn run_migrate(root: &Path, opts: &MigrateOptions) -> CoreResult<MigrateReport>;
pub fn format_migrate_human(r: &MigrateReport) -> String;
pub fn migrate_report_to_json(r: &MigrateReport) -> CoreResult<String>;
```

## Pré-condições

1. ProjectRoot válido
2. `validate_migrate_target(&opts.to_stack)`
3. `DARE/IDEIA.md` é ficheiro → senão `InvalidInput` `"run dare reverse first: missing DARE/IDEIA.md"`
4. Modules: `reverse-facts.json` `modules[].id` **ou** glob `DARE/REVERSE/module-*.md`; vazio → `"run dare reverse first: no modules"`

## Write (`check=false`)

Atomic writes **apenas**:

1. `DARE/MIGRATION/MIGRATION.md` — template BLUEPRINT §3.4 + AGENT markers vazios (`paradigm`, `strategy`, `risk-register`, `target-architecture`, `cutover-rollback`)
2. `DARE/MIGRATION/migration-facts.json` — `schemaVersion: 1`, camelCase
3. `DARE/MIGRATION/parity/{moduleId}.feature` — shape **exacto** §3.5 (skeleton only)

`written` relativo, sort lex. `redact` em evidence/detail.

## Check (`check=true`)

- `mode=check`, `written=[]`, **zero** creates/modifies sob `DARE/MIGRATION`
- Human contém indicação de check (ex. `MSG_CHECK` / `mode: check`)

## Edge cases

| Input | Resultado |
|-------|-----------|
| `--check` + IDEIA + modules | 0 writes |
| write 2× | overwrite atómico; parity = módulos atuais |
| path traversal | `InvalidInput` path safety |
| `opts.ai` | ignorado no domínio |

## Testes esperados

- `run_migrate_check_zero_write`
- `run_migrate_write_creates_md_facts_parity`
- `run_migrate_missing_ideia`
- `run_migrate_no_modules`
- `parity_feature_skeleton_shape`
- `format_migrate_human_contains_mode_stacks`

## Validation Gates

- [ ] `cargo test -p dare-project -- migrate`
- [ ] Path jail verificado

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()` / stubs
- [ ] Gherkin **não** inventa Given/When/Then de negócio
- [ ] Check realmente não escreve (assert filesystem)
- [ ] Facts JSON deserializa para `MigrateReport` / schema 1
- [ ] Mocks só em testes
