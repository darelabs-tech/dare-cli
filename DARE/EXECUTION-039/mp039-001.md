# Task mp039-001: Domain types + allowlist + compare + phases/gaps

> **Complexity:** MED · **Depends on:** — · **Rank:** 0

## Objetivo

Implementar o núcleo tipado de `dare-project::migrate` (sem I/O de write) conforme `DARE/BLUEPRINT-039-migrate.md` §0–§3.1.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-project/src/migrate.rs` |
| MODIFICAR | `crates/dare-project/src/lib.rs` → `pub mod migrate;` |

## Assinaturas / tipos (MUST)

```rust
pub const MIGRATE_SCHEMA_VERSION: u32 = 1;

pub struct MigrateOptions {
    pub to_stack: String,
    pub check: bool,
    pub ai: bool, // reserved; domain ignores
}

pub struct BlockingGap { /* id, severity, evidence, detail — camelCase serde */ }
pub struct MigrationPhase { /* id, title, modules, evidence */ }
pub struct MigrateReport { /* schema_version, mode, from_stacks, to_stack, to_family,
    comparison, phases, blocking_gaps, module_ids, written, warnings */ }

pub fn validate_migrate_target(to: &str) -> CoreResult<()>;
```

Allowlist exact: `node-nestjs`, `python-fastapi`, `php-laravel`, `go-gin`, `go-stdlib`, `rails`, `rust-axum`, `rust`, `rust-leptos`, `rust-leptos-csr`, `react`, `vue`, `mcp-node-ts`.

Comparison: `same_family` | `cross_stack` | `unknown_origin`.

Phases ids order: `foundations` → `modules` → `cutover`.

Gaps: `gap-unknown-origin` / `gap-stack-conflict` blocking; DNA/patterns ausentes = warning; sort `(severity desc, id asc)`.

## Edge cases (testar)

| Caso | Resultado |
|------|-----------|
| `to=""` ou `"Not-A-Stack"` | `InvalidInput` msg contém `unknown migrate target` ou `not in allowlist` |
| `to=" node-nestjs "` | trim → OK |
| origin unknown | `comparison=unknown_origin` + gap blocking |
| conflicts de detect | `gap-stack-conflict` |

## Testes esperados

- `validate_migrate_target_accepts_allowlist`
- `validate_migrate_target_rejects_unknown`
- `compare_families_same_cross_unknown`
- `phases_order_foundations_modules_cutover`
- `blocking_gaps_sort_blocking_then_id`

## Fora de escopo

- `run_migrate` writes / CLI / docs DEC

## Validation Gates

- [ ] `cargo test -p dare-project -- migrate`
- [ ] Sem `todo!()` / funções vazias públicas

## Definition of Done (ANTI-STUB)

- [ ] Nenhum `TODO`/`FIXME`/`unimplemented!()`/`todo!()`
- [ ] Nenhuma função pública vazia ou `Ok(())` sem lógica real de validação/compare
- [ ] Allowlist e comparison cobertos por asserts reais
- [ ] Mocks só em testes
