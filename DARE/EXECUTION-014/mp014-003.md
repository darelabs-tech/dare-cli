# Task mp014-003: install + validate + frontmatter + coexistência Codex

## Objetivo

Fase 3 — 49 commands + skills + frontmatter + Codex share.

## Assinaturas

```rust
pub fn install_antigravity(root: &ProjectRoot, force: bool) -> CoreResult<usize>;
pub fn validate_skill_frontmatter(body: &str) -> CoreResult<()>;
pub fn validate_antigravity_install(root: &ProjectRoot) -> CoreResult<usize>;
```

## Testes

- roundtrip force 49/49
- frontmatter reject incomplete / OK
- coexistência Codex após Antigravity

## Arquivos

- `crates/dare-harness/src/antigravity.rs`

## Validation Gates

- [ ] `cargo test -p dare-harness -- antigravity::`

## Definition of Done (ANTI-STUB)

- [ ] Assert 49; frontmatter real; coexistência real
- [ ] Sem git commit
