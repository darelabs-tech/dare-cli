# Task mp014-004: CLI help `--force` + install pipeline

## Objetivo

Fase 4 — help force + ordem install.

## Alterações

- `AntigravityCmd::Install`: docstring `--force` — unmanaged overwritten when set
- Ordem: `generate_antigravityrules` → `ensure_workflows_dir` → `install_antigravity`

## Arquivos

- `crates/dare-cli/src/main.rs`

## Validation Gates

- [ ] `cargo build -p dare-cli`

## Definition of Done (ANTI-STUB)

- [ ] Help `--force` explícito
- [ ] Sem git commit
