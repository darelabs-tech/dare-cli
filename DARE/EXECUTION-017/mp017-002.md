# Task mp017-002: Congelar collect_info + schema 1 + TASKS sort + read-only

> **Complexidade:** MED · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-017** §4–§5.1–5.4

## Objetivo

`collect_info` schema 1 estável, zero writes, sort lexico de `TASKS-*`.

## Arquivos

| Ação | Caminho |
|------|---------|
| MODIFICAR | `crates/dare-cli/src/commands/info.rs` |

## Contratos

- `INFO_SCHEMA_VERSION = 1`; campos camelCase congelados
- Root markers: `dare.config.json` | `DARE/` | `Cargo.toml`
- TASKS: prefer `DARE/TASKS.md`; senão `TASKS-*.md` **sorted** → first
- Heurística: `✅`+`DONE` / `⏳`+`PENDING`
- Human: linha `mode: read-only (zero mutations)`
- Backend: `ide` ou `backend` via `ProjectRoot`/`SafeRelativePath`
- Graph: `dare-graph.yml` ou `DARE/dare-graph.yml`

## Testes

- `find_root_walks_up`
- `collect_is_read_only_and_schema_stable`
- `tasks_picks_lexicographic_tasks_star` (**novo**)

## Validation Gates

- [ ] `cargo test -p dare-cli -- info`
- [ ] Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Sort lexico real (não `break` no primeiro)
- [ ] Zero writes testado
- [ ] Sem `todo!()` / stubs
