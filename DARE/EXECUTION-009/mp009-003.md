# Task mp009-003: Freshness hashes + templates README

## Objetivo

Garantir que `assets/manifest.yml` bate com bytes no FS (T-10) e documentar SoT.

## Descrição

### Teste

```rust
#[test]
fn manifest_hashes_match_assets_dir() { /* … */ }
```

1. `assets_root = CARGO_MANIFEST_DIR/../../assets`  
2. Ler `manifest.yml` do FS → `load_manifest_from_str`  
3. Para cada entry com `kind != External`: ler `assets_root.join(entry.path)`, `sha256_hex == entry.sha256`  

### README

`templates/README.md`:

- Source of truth: `assets/templates/`
- `templates/` na raiz = espelho legado (Class B)
- Não editar templates raiz como canónicos

## Arquivos

- `crates/dare-assets/src/*.rs` ou `tests/*.rs` (teste)
- `templates/README.md` (NOVO)

## Validation Gates

- [ ] Freshness test verde
- [ ] README presente
- [ ] `cargo test -p dare-assets`

## Definition of Done (ANTI-STUB)

- [ ] Compara hashes reais (não `assert!(true)`)
- [ ] Falha se um ficheiro do inventário mínimo divergir

## Próxima Task

mp009-005
