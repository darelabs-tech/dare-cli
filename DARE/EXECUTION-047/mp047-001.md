# Task mp047-001: clap + resolve flags (init/bootstrap)

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Registar `dare init` / `dare bootstrap` no CLI e implementar resolve de flags puro (BLUEPRINT-047 §5 / §0.1–0.3).

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `Cargo.toml` (pin `dialoguer = "=0.11.0"`) |
| MOD | `crates/dare-cli/src/main.rs` |
| MOD | `crates/dare-cli/src/commands/mod.rs` |
| CRIAR | `crates/dare-cli/src/commands/init.rs` |
| CRIAR | `crates/dare-cli/src/commands/bootstrap.rs` |

## MUST

- Flags: `--stack`, `--mcp`, `--fullstack`, `--transport`, `--toolchain`, `--non-interactive`, `--force`, `--check`, `-d`
- Mutual exclusion stack/mcp → exit 2 + `MSG_STACK_AND_MCP`
- Non-interactive sem name ou sem stack/mcp → exit 2
- `rails` → `ruby-rails-8`; mcp map §0.2; fullstack react|vue
- Backend + transport → InvalidInput `MSG_TRANSPORT_BACKEND`
- Reports types schemaVersion 1 camelCase
- Sem escrita FS real obrigatória neste rank (stubs ok se resolve testado)

## Testes

- `resolve_rails_alias`
- `resolve_mcp_map`
- `reject_stack_and_mcp`
- `reject_noninteractive_incomplete`
- `reject_transport_on_backend`

## Definition of Done (ANTI-STUB)

- [ ] Help mostra `init` e `bootstrap`
- [ ] Asserts reais nos resolve tests
- [ ] Sem `todo!()` em paths de parse/resolve
