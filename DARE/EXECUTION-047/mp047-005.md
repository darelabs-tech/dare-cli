# Task mp047-005: run_bootstrap + idempotência

> **Complexity:** HIGH · **Depends on:** mp047-001, mp047-002 · **Rank:** 1

## Objetivo

Implementar `run_bootstrap` com `SkipExisting` e `--force` (BLUEPRINT §5.2).

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-cli/src/commands/bootstrap.rs` |
| MOD | `crates/dare-cli/src/main.rs` (wire) |

## MUST

- Load config; NotFound `MSG_MISSING_CONFIG`; missing stack `MSG_MISSING_STACK_FIELD`
- `!force` → `SkipExisting`; `--force` → Replace
- `--toolchain` override persiste (preserve extras)
- Sem reinstall de harnesses
- `--check` zero writes
- Segunda execução `!force` → `created=[]`

## Testes

- `bootstrap_idempotent`
- `bootstrap_force_replaces`
- `bootstrap_missing_config`
- `bootstrap_check_zero_write`

## Definition of Done (ANTI-STUB)

- [ ] ×2 sem corrupção
- [ ] Toolchain override no disco
- [ ] Report `mode=bootstrap`
