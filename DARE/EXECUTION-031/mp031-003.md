# Task mp031-003: CodexDriver JSONL + suite comum

> **Complexidade:** HIGH · **Depends on:** mp031-002 · **Fase:** 3  
> **BLUEPRINT-031** T-15 T-19–T-22 · §4.3 · §5 · §9

## Objetivo

Implementar `CodexDriver` com defaults seguros, parse JSONL tolerante e suite comum (9 casos) via `ProcessRunner` mock.

## Arquivos

- `crates/dare-agent/src/drivers/codex.rs` (NOVO)
- `crates/dare-agent/src/drivers/mod.rs` (MOD)

## Assinaturas (MUST)

```rust
pub struct CodexDriver { /* … */ }
impl CodexDriver {
    pub fn from_env() -> CoreResult<Self>;
    pub fn from_env_with_runner(runner: Arc<dyn ProcessRunner>) -> CoreResult<Self>;
}
impl AgentDriver for CodexDriver { /* id="codex", doctor, run */ }
```

## Defaults (MUST)

```text
program: codex
args: exec --json --sandbox read-only --ask-for-approval never
# ou DARE_CODEX_COMMAND override completo
```

## Parse JSONL (MUST)

Seguir BLUEPRINT §4.3: eventos success/failure; `usage.total_tokens` / `tokens`; fallback exit 0 texto; malformed → Failure + `MSG_MALFORMED`.

## doctor / run (MUST)

- `doctor`: sempre `Ok(DriverHealth)`; missing → `ok=false`
- `run`: stdin=prompt; timeout 20min; missing → `Err` Internal `executable not found: …`
- `req.model` Some → append `--model {m}`

## Suite comum (MUST — 9)

doctor ok/missing · success · failure · timeout · cancel · malformed · missing exe · secret redaction

## Validation Gates

- [ ] `cargo test -p dare-agent`
- [ ] Sem `resolve_driver` wire (mp031-005)
- [ ] Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Sem SDK Anthropic / sem shell
- [ ] SafeCommand argv-only
- [ ] Suite 9 verde com mock runner

## Segurança

- RS-06 SafeCommand · RS-07 redact · T-15 sandbox read-only

## Próxima Task

mp031-005 (após mp031-004 também)
