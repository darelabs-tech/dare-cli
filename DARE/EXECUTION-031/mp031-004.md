# Task mp031-004: Claude/Cursor/Antigravity drivers + suite

> **Complexidade:** HIGH · **Depends on:** mp031-002 · **Fase:** 4  
> **BLUEPRINT-031** T-16–T-18 · §4.4 · §9

## Objetivo

Implementar três drivers de texto (`claude`, `cursor`, `antigravity`) com suite comum 9×3.

## Arquivos

- `crates/dare-agent/src/drivers/claude.rs` (NOVO)
- `crates/dare-agent/src/drivers/cursor.rs` (NOVO)
- `crates/dare-agent/src/drivers/antigravity.rs` (NOVO)
- `crates/dare-agent/src/drivers/mod.rs` (MOD)

## Defaults (MUST)

| Driver | program | args |
|--------|---------|------|
| claude | `claude` | `-p --output-format text` |
| cursor | `cursor-agent` | `--print` |
| antigravity | `antigravity` | `agent --print` |

Overrides: `DARE_CLAUDE_COMMAND` / `DARE_CURSOR_COMMAND` / `DARE_ANTIGRAVITY_COMMAND`.

## Parse (MUST)

BLUEPRINT §4.4: exit 0 → Success; ≠0 → Failure; timeout/cancel; tokens regex opcional.  
`supports_model_flag`: claude=true; cursor/antigravity=false.

## Suite comum (MUST)

9 casos × 3 drivers com `ProcessRunner` mock.

## Validation Gates

- [ ] `cargo test -p dare-agent`
- [ ] Sem wire `resolve_driver` (mp031-005)
- [ ] Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Três structs `*Driver` + `AgentDriver` impl
- [ ] Sem Anthropic SDK
- [ ] Suites verdes

## Segurança

- RS-06 argv-only · RS-07 redact

## Próxima Task

mp031-005
