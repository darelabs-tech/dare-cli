# Task mp031-002: drivers/argv + common finalize

> **Complexidade:** MED · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-031** §0 T-02 T-03 T-14 · §0.3 · §3

## Objetivo

Criar módulo `drivers/` com `parse_argv_override` e `finalize_result` (redact/truncate), sem drivers reais ainda.

## Arquivos

- `crates/dare-agent/src/drivers/mod.rs` (NOVO)
- `crates/dare-agent/src/drivers/argv.rs` (NOVO)
- `crates/dare-agent/src/drivers/common.rs` (NOVO)
- `crates/dare-agent/src/lib.rs` (MOD: `mod drivers`)

## Assinaturas (MUST)

```rust
pub fn parse_argv_override(env_val: &str) -> CoreResult<(String, Vec<String>)>;
// empty trim → InvalidInput "command override must not be empty"

pub const ENV_CODEX: &str = "DARE_CODEX_COMMAND";
pub const ENV_CLAUDE: &str = "DARE_CLAUDE_COMMAND";
pub const ENV_CURSOR: &str = "DARE_CURSOR_COMMAND";
pub const ENV_ANTIGRAVITY: &str = "DARE_ANTIGRAVITY_COMMAND";
pub const AGENT_DRIVER_TIMEOUT: Duration = Duration::from_secs(20 * 60);
pub const SUMMARY_MAX_CHARS: usize = 512;
pub const MSG_EXEC_NOT_FOUND: &str = "executable not found: {program}"; // format helper
pub const MSG_MALFORMED: &str = "malformed driver output";

// common: redact + truncate_chars em stdout/stderr/summary; map timeout/cancel
```

## Restrições

- **NÃO** depender de `dare-ai`
- **NÃO** implementar Codex/Claude/Cursor/Antigravity ainda

## Validation Gates

- [ ] `cargo test -p dare-agent` (argv empty/valid; finalize redacts Bearer)
- [ ] Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!` / `unimplemented!`
- [ ] Testes cobrem override vazio e secret redaction

## Segurança

- RS-05 env-only · RS-07 redact em finalize

## Próxima Task

mp031-003 ∥ mp031-004
