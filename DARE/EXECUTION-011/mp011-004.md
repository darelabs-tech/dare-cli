# Task mp011-004: settings.json + PostToolUse + help `--force`

## Objetivo

Fase 4 — settings managed + hook fixo + CLI install completo + help force.

## Assinatura

```rust
pub fn write_settings_json(root: &ProjectRoot, force: bool) -> CoreResult<()>;
```

## Settings mínimos

- `_dare_managed: true`
- `hooks.PostToolUse[0].matcher` = `"Write"`
- `hooks.PostToolUse[0].hooks[0].type` = `"command"`
- `command` **exato** (Blueprint §5.4):

```text
echo "File saved. Remember Ralph Loop: cargo test --workspace && cargo clippy --all-features -- -D warnings"
```

## Preserve

- Settings existem **sem** `_dare_managed` + `!force` → no-op
- Managed ou force → rewrite default

## CLI (`dare-cli`)

Ordem install: `generate_claude_md` → `install_commands` → `write_settings_json`

Clap `--force`: docstring/help deve mencionar que unmanaged files are overwritten.

## Testes

- settings JSON parseável; contém `_dare_managed` e `PostToolUse`
- preserve unmanaged settings

## Arquivos

- `crates/dare-harness/src/claude.rs`
- `crates/dare-cli/src/main.rs`

## Validation Gates

- [ ] `cargo test -p dare-harness -- claude::`
- [ ] `cargo build -p dare-cli`

## Definition of Done (ANTI-STUB)

- [ ] Command PostToolUse constante (sem interpolação user)
- [ ] Help `--force` explícito
- [ ] Sem git commit
