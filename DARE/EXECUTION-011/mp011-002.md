# Task mp011-002: Congelar detect + generate_claude_md + preserve

## Objetivo

Congelar contratos BLUEPRINT-011 §5.1–5.2 em `dare-harness::claude`.

## Assinaturas

```rust
pub struct ClaudeDetect { pub claude_md: bool, pub claude_dir: bool }
pub fn detect_claude(root: &ProjectRoot) -> CoreResult<ClaudeDetect>;
pub fn generate_claude_md(root: &ProjectRoot, force: bool) -> CoreResult<()>;
```

## Regras

1. `detect_claude` — só lê existência de `CLAUDE.md` e `.claude/`; **zero writes**
2. `generate_claude_md` — 1ª linha `<!-- dare:managed …`; body stub §5.2 do Blueprint
3. Preserve: ficheiro existe, não managed, `!force` → no-op Ok
4. Managed ou `force` → rewrite stub

## Testes

- detect em tempdir vazio → ambos false
- generate → ficheiro managed
- CLAUDE.md custom sem marcador + `force=false` → conteúdo intacto

## Arquivos

- `crates/dare-harness/src/claude.rs`

## Validation Gates

- [ ] `cargo test -p dare-harness -- claude::`

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()` / stubs vazios
- [ ] Assinaturas públicas estáveis
- [ ] Sem git commit
