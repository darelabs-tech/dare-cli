# Task mp011-005: CLI smoke + docs DEC-012 + golden SHOULD

## Objetivo

Fase 5 — smoke end-to-end + documentação DEC-012.

## Smoke (`cli_smoke` ou equivalente)

1. tempdir como `--root`
2. `dare harness claude install --force --root <tmp>` → stdout contém `wrote 49`
3. `dare harness claude validate --root <tmp>` → `ok` + `49`
4. `dare harness claude detect --root <tmp>` → `claude_md=true` (e `claude_dir=true` após install)

## Docs

Expandir `docs/compatibility/harness-claude.md`:

- CLI detect/install/validate
- Preserve (md + settings)
- Settings schema + PostToolUse
- T-01…T-14 (resumo)
- RS-01…RS-08 map
- DEC-012

SHOULD: nota de que os 49 paths = `outputs.claude` da matrix; drift vs TS 3.18.1 = Classe B/C documentada.

Verificar `docs/DECISION-LOG.md` DEC-012 active. **Não** implementar Cursor/Codex/Antigravity.

## Arquivos

- `crates/dare-cli/tests/cli_smoke.rs` (ou novo teste)
- `docs/compatibility/harness-claude.md`

## Validation Gates

- [ ] `cargo test -p dare-cli --test cli_smoke`

## Definition of Done (ANTI-STUB)

- [ ] Smoke real (binário), não assertTrue(true)
- [ ] Docs com DEC-012
- [ ] Sem git commit
