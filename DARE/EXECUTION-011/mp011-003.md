# Task mp011-003: install_commands + validate_install

## Objetivo

Fase 3 — instalar 49 commands da matriz e validar presença; preserve unmanaged.

## Assinaturas

```rust
pub fn install_commands(root: &ProjectRoot, force: bool) -> CoreResult<usize>;
pub fn validate_install(root: &ProjectRoot) -> CoreResult<usize>;
```

## Algoritmo install

1. Load embed `capability-matrix.yml` → validate
2. Para cada `outputs.claude` Some:
   - Se `!should_write` → skip
   - Body = `<!-- dare:managed capability={id} -->\n` + `render_claude_command(cap)`
   - `atomic_write`
3. Return nº escritos

## Validate

- Todos os paths Claude existem como ficheiro → `Ok(n)` com n=49 happy path
- Missing → `Err(Config)` com `(count):` + até 5 paths
- **Não** apaga ficheiros; **não** exige marcador managed

## Testes

- `install_validate_roundtrip_force`: written=49, validate=49
- `preserve_unmanaged_command`: 48 escritos; conteúdo user intacto
- Re-install managed: written=49 (rewrite idempotente)

## Arquivos

- `crates/dare-harness/src/claude.rs`

## Validation Gates

- [ ] `cargo test -p dare-harness -- claude::`

## Definition of Done (ANTI-STUB)

- [ ] Assert 49 no force path
- [ ] Preserve real (não mock)
- [ ] Sem git commit
