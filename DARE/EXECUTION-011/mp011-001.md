# Task mp011-001: Verificar docker-compose.ci.yml

## Objetivo

Fase 1 — confirmar containerização CI parseável (sem criar Dockerfile novo se já existe).

## Passos

1. Correr `docker compose -f docker-compose.ci.yml config`
2. Se Docker ausente: abrir o YAML e confirmar sintaxe básica; documentar no `--output`

## Definition of Done (ANTI-STUB)

- [ ] `config` exit 0 **ou** YAML documentado como parseável sem Docker
- [ ] Sem alterações de código necessárias
- [ ] Sem git commit
