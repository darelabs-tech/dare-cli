# Task mp054-002: Normalizer N-01..N-08 + anti over-normalize

> **Complexity:** HIGH · **Depends on:** mp054-001 · **Rank:** 1

## Objetivo

Ao final, `normalize_text` aplica só a allowlist §0.3 e testes anti-cheat falham se exit/key de contrato mudam.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-parity/src/normalize.rs` |
| MOD | `lib.rs` |
| CRIAR | `crates/dare-parity/tests/normalize_anti_cheat.rs` |

## MUST

```rust
pub struct NormalizeCtx { pub temp_prefixes: Vec<PathBuf>, pub binary_version: Option<String> }
pub fn normalize_text(input: &str, ctx: &NormalizeCtx) -> String;
```

| Id | Transformação |
|----|----------------|
| N-01 | ISO-8601 → `1970-01-01T00:00:00Z` |
| N-02 | UUID v4 → `00000000-0000-4000-8000-000000000000` |
| N-03 | temp prefixes → `$TMP/` |
| N-04 | strip ANSI CSI |
| N-05 | `\` → `/` |
| N-06 | drive `X:` → `$DRIVE:` |
| N-07 | binary version → `$VERSION` |
| N-08 | `dare_core::redact` → `$REDACTED` |

**Proibido:** normalizar exit codes, nomes de flags, chaves JSON de contrato.

## Testes

- Um unit por N-01..N-08
- `over_normalize_does_not_hide_exit_code`
- `over_normalize_does_not_hide_json_key`

## Definition of Done (ANTI-STUB)

- [ ] Allowlist fechada — sem “normalize everything”
- [ ] Anti-cheat vermelho se contrato muda
- [ ] `cargo test -p dare-parity` verde
