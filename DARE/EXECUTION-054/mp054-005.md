# Task mp054-005: proptest paths/parsers + cross-platform

> **Complexity:** MED · **Depends on:** mp054-001 · **Rank:** 1

## Objetivo

Ao final, property tests (256 cases) não panicam e `cross_platform` cobre separators/drive casing.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-parity/src/{fuzz_paths,fuzz_parsers}.rs` (cfg test ok) |
| CRIAR | `crates/dare-parity/tests/cross_platform.rs` |
| CRIAR | `tests/cross-platform/windows-path-cases/case.yaml` (+ expected se aplicável) |
| MOD | `tests/cross-platform/README.md` (nota cargo-fuzz SHOULD) |

## MUST

```rust
// proptest Config::with_cases(256)
fn safe_relative_path_no_panic(s: String) { let _ = SafeRelativePath::new(&s); }
fn yaml_case_parse_no_panic(bytes: Vec<u8>) { let _ = CaseSpec::try_from_yaml_bytes(&bytes); }
```

Malformed → `Err`, nunca panic. Documentar `cargo fuzz` como SHOULD (não instalar obrigatório).

## Testes

- proptest no `cargo test -p dare-parity`
- `cross_platform` passa (cfg windows/unix conforme necessário)

## Definition of Done (ANTI-STUB)

- [ ] 256 cases configurados
- [ ] Sem bypass de path jail “para passar”
- [ ] README menciona nightly fuzz SHOULD
