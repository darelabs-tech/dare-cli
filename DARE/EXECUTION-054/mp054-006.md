# Task mp054-006: Perf scripts + baseline stub + diff-log skeleton

> **Complexity:** MED · **Depends on:** — · **Rank:** 0

## Objetivo

Ao final, scripts de medida existem, `docs/perf/baseline-054.md` tem front-matter, e `parity-diff-log.md` tem tabela inicial — **sem** tocar `crates/dare-parity`.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `scripts/measure-perf.sh` |
| CRIAR | `scripts/measure-perf.ps1` |
| CRIAR | `docs/perf/baseline-054.md` |
| CRIAR | `docs/compatibility/parity-diff-log.md` |

## MUST

Scripts:
1. `cargo build -p dare-cli --release`
2. 5× `dare --version` — descartar 1º; mediana ms
3. RSS (Windows WorkingSet / Unix `ps`)
4. `binarySizeBytes` + sha256
5. Escrever/atualizar front-matter `schemaVersion: 1` em baseline-054.md

Comentários: `PERF_REGRESSION_MAX=0.15`.  
Diff-log: header de tabela + ≥1 row exemplo Class C (ex. design LLM SoT nativo).

**Não** modificar `crates/**` (mp054-001 é dono).

## Testes

- Scripts existem e são invocáveis (smoke manual OK); validação numérica do gate fica nos units de `perf.rs` (001)

## Definition of Done (ANTI-STUB)

- [ ] Scripts reais (não placeholder vazio)
- [ ] Front-matter YAML válido no baseline
- [ ] Zero conflitos com crate dare-parity
