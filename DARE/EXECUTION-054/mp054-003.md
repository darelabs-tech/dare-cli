# Task mp054-003: Golden runner + fixtures + diff-log

> **Complexity:** HIGH · **Depends on:** mp054-002 · **Rank:** 2

## Objetivo

Ao final, `run_suite` executa cases YAML commitados e `cargo test -p dare-parity --test golden_suite` passa.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-parity/src/{runner,report,diff_log}.rs` |
| CRIAR | `crates/dare-parity/tests/golden_suite.rs` |
| CRIAR | `tests/golden/cases/**/case.yaml` + `expected/*` |
| CRIAR | `tests/fixtures/<fixture_id>/` stubs mínimos |
| MOD | `docs/compatibility/parity-diff-log.md` (rows reais se skip C) |
| MOD | `.gitignore` (`tests/golden/last-report.json` opcional) |

## MUST

```rust
pub fn run_case(spec: &CaseSpec, bin: &Path, fixtures_root: &Path, diff_log: &DiffLogIndex) -> CoreResult<CaseResult>;
pub fn run_suite(golden_root: &Path, opts: SuiteOpts) -> CoreResult<DiffReport>; // schemaVersion 1
```

Pipeline: skip classificado → spawn `assert_cmd` argv-only timeout 30s → compare axes com `normalize_text`.

Cases MUST mínimos: `dare --help`, `welcome --help`, `self --help`, `mcp --help`. HTTP health SHOULD se barato.

Sem npm/live TS na CI — snapshots only.

## Testes

- `golden_suite` integration verde
- skip sem class → fail `MSG_SKIP_NEEDS_CLASS`
- unclassified Class C sem diff-log → `MSG_UNCLASSIFIED_DIFF`

## Definition of Done (ANTI-STUB)

- [ ] Runner real (não `assert!(true)`)
- [ ] Fixtures sem secrets reais
- [ ] Skips sempre com `class`
