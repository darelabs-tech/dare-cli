# Task mp054-004: Security suite injection/leak/archive/sig/bidi

> **Complexity:** HIGH · **Depends on:** mp054-001 · **Rank:** 1

## Objetivo

Ao final, `cargo test -p dare-parity --test security_suite` cobre CI-010..014 relevantes sem reimplementar extract.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-parity/src/security/{mod,injection,env_leak,archive,signature,bidi}.rs` |
| CRIAR | `crates/dare-parity/tests/security_suite.rs` |
| CRIAR | `tests/security/injection/payloads.txt` |
| CRIAR | `tests/security/archives/{zip-slip.zip,tar-slip.tar.gz}` |
| CRIAR | `tests/security/signatures/{bad-SHA256SUMS,signing-skipped.sig}` |

## MUST

```rust
pub fn test_command_injection_payloads(runner: &dyn ProcessRunner) -> CoreResult<()>;
pub fn test_env_leak_absent(bin: &Path, secret: &str) -> CoreResult<()>;
pub fn test_archive_traversal_fixtures(dir: &Path) -> CoreResult<()>;
pub fn test_signature_mismatch_fixtures(dir: &Path) -> CoreResult<()>;
pub fn test_bidi_path_rejected(root: &ProjectRoot) -> CoreResult<()>;
```

- payloads.txt: `;rm -rf /`, `&& whoami`, `$(reboot)`, `` `id` ``, `arg%0anewline`, `| cat /etc/passwd`
- Archive: **somente** `dare_skills::extract_archive_safe`
- Sig: `dare_self::verify_sha256` + signing skipped → msgs Blueprint 053
- Env: `GITHUB_TOKEN=ghp_TESTONLY_LEAK_PROBE`, `DARE_AI_API_KEY=sk_TESTONLY` ausentes do output
- Bidi: U+202E / path escape → Err

## Testes

- `security_suite` cobre as 5 superfícies MUST

## Definition of Done (ANTI-STUB)

- [ ] Sem shell concatenado
- [ ] Sem reimplementar zip extract
- [ ] Placeholders only (sem tokens reais)
