# Task mp054-001: Scaffold dare-parity + CaseSpec + tests/ layout

> **Complexity:** MED · **Depends on:** — · **Rank:** 0

## Objetivo

Ao final, `crates/dare-parity` está no workspace com `CompareAxis`, `CaseSpec`/`validate_case`, `within_regression` e dirs `tests/{golden,security,cross-platform}` com README.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-parity/Cargo.toml` |
| CRIAR | `crates/dare-parity/src/{lib,axis,case,perf}.rs` |
| MOD | `Cargo.toml` (workspace members + path deps) |
| CRIAR | `tests/golden/README.md`, `tests/security/README.md`, `tests/cross-platform/README.md`, `tests/fixtures/.gitkeep` |

## MUST

```rust
pub enum CompareAxis { Exit, Stdout, Stderr, Tree, Content, State, Http } // serde lowercase
pub fn load_case(case_dir: &Path) -> CoreResult<CaseSpec>;
pub fn validate_case(spec: &CaseSpec) -> CoreResult<()>;
pub fn within_regression(baseline: f64, measured: f64, max_ratio: f64) -> bool; // default max 0.15
```

- `CASE_SCHEMA_VERSION = 1`; id regex Blueprint §0.4
- `MSG_SKIP_NEEDS_CLASS` se skip sem class
- Constantes `PERF_REGRESSION_MAX = 0.15`

## Testes

- `case_rejects_bad_schema_version`
- `skip_without_class_fails_validate`
- `regression_over_15_percent_fails`

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()`; `cargo test -p dare-parity` verde
- [ ] Sem normalize/runner/security modules ainda
- [ ] Nenhum secret em fixtures
