# Task mp015-005: Smoke instalação limpa + docs DEC-016 + ADR-008

> **Complexidade:** MED  
> **Depends on:** mp015-003, mp015-004  
> **Fase:** 5 — Smoke + docs (BLUEPRINT-015 §5.5)

## Objetivo

Smoke local prova package→install→`--version`; docs e ADR-008 fecham DEC-016.

## Arquivos

| Ação | Caminho |
|------|---------|
| MODIFICAR | `scripts/smoke-release-install.sh` |
| MODIFICAR | `scripts/smoke-release-install.ps1` (se aplicável) |
| MODIFICAR | `docs/compatibility/release-alpha.md` |
| CRIAR | `docs/adr/ADR-008-release-alpha-nativo.md` |
| VERIFICAR | `docs/DECISION-LOG.md` (DEC-016 → ADR-008) |

## Smoke (MUST)

1. `cargo build -p dare-cli --release`  
2. Package host → `dist/smoke/dare-${VERSION}-${HOST}.{tar.gz\|zip}`  
3. `SHA256SUMS` + `SBOM.spdx.json` mínimo + `SHA256SUMS.sig` skip  
4. `DARE_LOCAL_ARCHIVE` + `DARE_PREFIX=dist/smoke/prefix` → installer  
5. Assert `--version` exit 0 e stdout match `^dare ` (**não** só `--help`)  
6. Assert meta files existem  
7. Opcional: grep 5 triples em `release.yml`

## Docs release-alpha.md

Incluir: triggers, matrix+runners, naming, SHA256SUMS/SBOM/sig, cosign soft, env installers, smoke, canal npm paralelo (não substituído).

## ADR-008

Decisões: alpha nativo sem npm; SBOM mínimo; cosign soft-fail; clap vs tag; cross aarch64; macos-13/14.

## Validation Gates

- [ ] `bash scripts/smoke-release-install.sh` exit 0 (ou ps1 no Windows)
- [ ] ADR-008 existe
- [ ] release-alpha.md não é stub de 5 linhas
- [ ] Sem git commit / sem tags
- [ ] NÃO implementar welcome 016

## Definition of Done (ANTI-STUB)

- [ ] Smoke real com `--version`
- [ ] ADR com decisões concretas (não “TBD”)
- [ ] Sem git commit

## Próxima Task

mp015-006
