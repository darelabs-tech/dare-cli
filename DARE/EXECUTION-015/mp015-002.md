# Task mp015-002: Congelar release.yml matrix 5 + package + dry_run

> **Complexidade:** MED  
> **Depends on:** —  
> **Fase:** 2 — Workflow (BLUEPRINT-015 §4.1–4.2 §5.1–5.2)

## Objetivo

`release.yml` com exactamente 5 targets, runners macOS 13/14, package naming canónico e dry_run default true.

## Arquivos

| Ação | Caminho |
|------|---------|
| MODIFICAR | `.github/workflows/release.yml` |

## Matrix (obrigatória)

| target | os | archive | cross |
|--------|-----|---------|-------|
| `x86_64-unknown-linux-gnu` | `ubuntu-latest` | tar.gz | — |
| `aarch64-unknown-linux-gnu` | `ubuntu-latest` | tar.gz | true |
| `x86_64-apple-darwin` | `macos-13` | tar.gz | — |
| `aarch64-apple-darwin` | `macos-14` | tar.gz | — |
| `x86_64-pc-windows-msvc` | `windows-latest` | zip | — |

## Package

- Stage: `dare-${VERSION}-${TARGET}` com só o binário
- Artifact: `.tar.gz` ou `.zip`
- `VERSION=${GITHUB_REF_NAME:-dev}`
- `fail-fast: false`

## Triggers

- tags: `v*-alpha*`, `v*-alpha.*`
- `workflow_dispatch` input `dry_run` boolean **default true**

## Edge cases

- Tag sem alpha → workflow não dispara
- Um target falha → outros continuam

## Validation Gates

- [ ] Grep: cinco triples + `macos-13` + `macos-14` + `dry_run`
- [ ] **Não** usar `macos-latest` para darwin targets
- [ ] Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Matrix completa (não placeholder)
- [ ] Package step real (tar/zip)
- [ ] Sem git commit

## Próxima Task

mp015-003
