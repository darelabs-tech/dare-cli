# Task mp043-004: neo4j feature + HTTP client

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Feature Cargo `neo4j` (não-default) + cliente HTTP + config/open_graph.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-graph/src/neo4j.rs` |
| MOD | `config.rs`, `lib.rs`, `Cargo.toml` (graph+cli) |

## MUST

- Sem feature: backend neo4j → InvalidInput requires feature
- Com feature: ureq POST tx/commit; timeout 5s; retries 2; backoff 100ms
- Read-only KG; writes Unsupported/InvalidInput 043
- Env `NEO4J_*`; scheme http/https; never log password

## Testes

- reject without feature
- bad scheme
- retry/timeout with mock (`#[ignore]` real Neo4j)

## Definition of Done (ANTI-STUB)

- [ ] Feature not in default
- [ ] Secrets redacted
- [ ] Sem Cypher arbitrário do user
