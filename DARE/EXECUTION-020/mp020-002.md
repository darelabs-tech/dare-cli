# Task mp020-002: Scaffold crate dare-dag + ValidationReport schema 1

> **Complexidade:** MED · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-020** §2–4 · T-01 T-08

## Objetivo

Criar workspace member `dare-dag` com tipos públicos do report (schema 1), sem regras de grafo ainda.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `Cargo.toml` (members + workspace.dependencies) |
| CRIAR | `crates/dare-dag/Cargo.toml` |
| CRIAR | `crates/dare-dag/src/lib.rs` |
| CRIAR | `crates/dare-dag/src/report.rs` |

## Contratos

- `VALIDATION_SCHEMA_VERSION = 1`; `MSG_MAX = 200`; `DEFAULT_DAG_REL = "DARE/dare-dag.yaml"`
- `COMPLEXITY_ALLOWED = ["LOW", "MED", "HIGH"]`
- `ValidateOptions { strict: bool }`
- `IssueSeverity`: JSON `"error"` \| `"warning"`
- `ValidationIssue`: `severity`, `code`, `taskId`, `message`, `path?`
- `ValidationReport`: `schemaVersion`, `mode` (`"validate"`), `ok`, `dagPath`, `format`, `taskCount`, `errorCount`, `warningCount`, `strict`, `issues`
- Deps crate: **só** `dare-core`, `dare-contracts`, `serde`, `serde_json`

## Testes

- `report_schema_version_1` — roundtrip JSON camelCase; `schemaVersion == 1`

## Validation Gates

- [ ] `cargo test -p dare-dag`
- [ ] Sem `validate.rs` completo (regras → mp020-003)
- [ ] Sem CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Tipos reais serializáveis (não stub)
- [ ] Sem `todo!()` em paths públicos
- [ ] Sem git commit
