# Task mp018-003: Git + monorepo + harnesses + detect() + read-only

> **Complexidade:** HIGH · **Depends on:** mp018-002 · **Fase:** 3  
> **BLUEPRINT-018** §5.2 §5.4–5.8

## Objetivo

`detect()` completo: git, monorepo, 4 harnesses, zero writes.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR/MOD | `crates/dare-project/src/{monorepo,harnesses,detect}.rs` |
| MOD | `root.rs` (git), `lib.rs`, `Cargo.toml` (+ dare-harness) |

## Contratos

- `find_git_root`: walk `.git`; opcional `SafeCommand` git; degradar None
- Monorepo: workspace markers + filhos depth≤3
- Harnesses: antigravity/claude/codex/cursor sorted
- `detect`: NotFound se dir missing; `mode=check`
- Human: linha final `mode: check (zero mutations)`

## Testes

monorepo pnpm; not monorepo single; harnesses four; read-only; git dir; git missing

## Validation Gates

- [ ] `cargo test -p dare-project`
- [ ] Sem git commit · Sem CLI

## Definition of Done (ANTI-STUB)

- [ ] Orquestrador completo
- [ ] Zero writes testado
- [ ] Sem git commit
