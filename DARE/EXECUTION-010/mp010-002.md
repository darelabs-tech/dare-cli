# Task mp010-002: Harden validate + exceptions YAML

## Objetivo

Fechar gaps de validate (regex kebab, path safety) e registar exceptions Classe C (T-04).

## Assinatura

```rust
pub fn validate_capability_matrix(m: &CapabilityMatrix) -> CoreResult<()>;
```

## Regras novas

1. `id` ∈ `^[a-z0-9]+(-[a-z0-9]+)*$`
2. Cada path Some em `outputs.*` → `assert_safe_asset_path`
3. Cada `assets[]` → `assert_safe_asset_path`
4. Exceptions: `id` + `reason` non-empty

## YAML exceptions (obrigatório)

```yaml
exceptions:
  - id: cursor-commands-full-parity
    reason: "Cursor command count 33 vs matrix deferred to adapter 012; Class C"
  - id: cursor-rules-full-parity
    reason: "Cursor rules count 25 not modeled as separate Capability rows; Class C"
  - id: agent-skills-full-parity
    reason: "Agent Skills count 48 package skills != IDE capabilities; Class C until 044/012"
```

Após editar YAML: `python scripts/regen-assets-manifest.py`

## Testes

- `rejects_underscore_id`
- `rejects_dotdot_output_path`

## Arquivos

- `crates/dare-assets/src/capability.rs`
- `assets/capability-matrix.yml`
- `assets/manifest.yml` (hash)

## Validation Gates

- [ ] `cargo test -p dare-assets`
- [ ] verify_embedded ainda OK

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()`
- [ ] Exceptions presentes no YAML embutido
