# Task mp029-003: Verification writer

> **Complexidade:** MED · **Depends on:** mp029-002 · **Fase:** 3  
> **BLUEPRINT-029** §4.4 · T-15 T-21

## Objetivo

Persistir `VerificationReport` em `.dare/verification/<id>.json` com jail + redact.

## Arquivos

- `crates/dare-verify/src/verification.rs` (NOVO)
- exports em `lib.rs`

## Assinaturas (MUST)

```rust
pub const VERIFICATION_DIR_REL: &str = ".dare/verification";
pub fn task_id_is_path_safe(id: &str) -> bool;
pub fn write_verification(root: &ProjectRoot, report: &VerificationReport) -> CoreResult<()>;
pub fn verification_from_ralph(task_id: &str, ralph: &RalphReport, updated_at: &str) -> VerificationReport;
```

`schema_version` sempre **1**. Id unsafe → InvalidInput.

## Validation Gates

- [ ] Roundtrip FS + unsafe id rejeitado
- [ ] `cargo test -p dare-verify`
- [ ] Sem CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] `atomic_write` real · redact em tails
- [ ] Cria diretório se ausente

## Segurança

- RS-01 jail · RS-02 redact

## Próxima Task

mp029-004
