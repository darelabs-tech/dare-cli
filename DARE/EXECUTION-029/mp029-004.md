# Task mp029-004: CLI `--complete` + smokes Ralph mock

> **Complexidade:** HIGH · **Depends on:** mp029-003 · **Fase:** 4  
> **BLUEPRINT-029** §5 · Apêndice C/E · T-06–T-10  
> **Status:** ✅ DONE

## Objetivo

`--complete <id>` com auto-Start, Ralph, bloqueio de DONE, exits 1/124, mock via env.

## Arquivos

- `crates/dare-cli/src/commands/execute.rs` (MOD)
- `crates/dare-cli/src/main.rs` (MOD clap)
- `crates/dare-cli/Cargo.toml` (dep `dare-verify`)
- `crates/dare-cli/tests/cli_smoke.rs` (`execute_complete_*`)

## Fluxo Complete (MUST)

1. ensure_state → task ∈ DAG  
2. PENDING → `Start` (sem canvas)  
3. `run_ralph` (`DARE_RALPH_MOCK` = pass|fail|timeout)  
4. `write_verification`  
5. fail/timeout → **sem** Complete; RUNNING; exit 1/124  
6. ok → Complete + canvas + verification ok=true  

Default `--output`: `Task completed.`

## Validation Gates

- [x] Smokes: pass→DONE; fail→RUNNING+1; timeout→124; missing→3; exclusive→2
- [x] `cargo test -p dare-cli --test cli_smoke -- execute_complete`
- [x] Sem docs/DEC · Sem git commit · Sem `--agent`

## Definition of Done (ANTI-STUB)

- [x] Sem `unimplemented!` no path Complete
- [x] Cada smoke MUST passa
- [x] Gate falho **nunca** marca DONE

## Segurança

- RS-01/08/09 · RS-06 via verify

## Notas de execução

- `--fail` / `--reset` também wired (completo, não stub) para clap + transição.
- Timeout usa `ExitCode::from(124)` após `write_error` (fora do mapa ErrorKind 1–5).
- `DARE_RALPH_MOCK`: unset→System; `1`/`pass`→3×exit0; `fail`→build 1; `timeout`→124.

## Próxima Task

mp029-005
