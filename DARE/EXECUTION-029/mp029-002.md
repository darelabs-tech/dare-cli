# Task mp029-002: Crate dare-verify (stacks + run_ralph)

> **Complexidade:** HIGH · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-029** §4.1–4.3 · T-01–T-14 · §5.2

## Objetivo

Crate `dare-verify` com resolução de stack, argv dos gates e `run_ralph` testável via mock.

## Arquivos

- `crates/dare-verify/Cargo.toml` (NOVO)
- `crates/dare-verify/src/{lib,ralph,stacks}.rs` (NOVO)
- `Cargo.toml` workspace members

## Assinaturas (MUST)

```rust
pub fn resolve_stack(root: &ProjectRoot) -> CoreResult<String>;
pub fn gate_commands(stack: &str) -> CoreResult<Vec<(GateAspect, SafeCommand)>>;
pub fn run_ralph(
    root: &ProjectRoot,
    stack: &str,
    runner: &dyn ProcessRunner,
    stdout_cap_chars: usize,
) -> CoreResult<RalphReport>;
```

## rust-axum gates (MUST)

| Aspect | argv |
|--------|------|
| build | `cargo build --workspace` |
| test | `cargo test --workspace` |
| lint | `cargo clippy --workspace --all-targets -- -D warnings` |

Timeout **600 s** por comando. Short-circuit no primeiro ≠0 / 124.

## Validation Gates

- [x] `cargo test -p dare-verify`
- [x] Sem CLI · Sem `verification.rs` · Sem git commit

## Definition of Done (ANTI-STUB)

- [x] Sem `todo!` / `unimplemented!` em APIs públicas
- [x] Mock tests: pass / fail short-circuit / timeout 124 / not implemented
- [x] Sem dependência `dare-dag`

## Segurança

- RS-06 argv-only · RS-07 timeout 600s

## Próxima Task

mp029-003
