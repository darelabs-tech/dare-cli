# Task mp030-002: Crate dare-agent (driver/mock/budget/policy/sig)

> **Complexidade:** HIGH · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-030** §0 T-01–T-13 · T-27–T-28 · §4.1–4.6 · §5.2

## Objetivo

Crate `dare-agent` com `AgentDriver` sync, `MockDriver`, `BudgetTracker`, política `fixed` e `failure_signature`.

## Arquivos

- `crates/dare-agent/Cargo.toml` (NOVO)
- `crates/dare-agent/src/{lib,driver,mock,budget,policy,signature}.rs` (NOVO)
- `Cargo.toml` workspace members

## Assinaturas (MUST)

```rust
pub trait AgentDriver: Send + Sync {
    fn id(&self) -> &str;
    fn doctor(&self) -> DriverHealth;
    fn run(&self, req: &AgentRequest, cancel: &CancelFlag) -> AgentRunResult;
}

pub fn resolve_driver(id: &str) -> CoreResult<Box<dyn AgentDriver>>;
// mock | noop → MockDriver; outros → Err("driver not implemented: …")

pub struct BudgetTracker { /* new(0)=unlimited; consume; can_continue */ }
pub fn apply_fixed(status: AgentRunStatus, attempt_n: u32, max: u32) -> FixedDecision;
pub fn failure_signature(aspect: &str, stderr: &str) -> String; // 8 hex
```

## Env mock (MUST)

`DARE_AGENT_MOCK`: `success` | `fail` | `timeout` (default `success`).

## Validation Gates

- [ ] `cargo test -p dare-agent`
- [ ] Sem `worktree.rs` · Sem CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!` / `unimplemented!` em APIs públicas
- [ ] Testes: mock success/fail/timeout/cancel; budget 0=∞ + exhaust; apply_fixed matrix; signature golden
- [ ] Sem deps `dare-cli` / `dare-dag`

## Segurança

- RS-02 redact em resultados · RS-07 budget + max attempts

## Próxima Task

mp030-003
