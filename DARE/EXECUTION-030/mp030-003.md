# Task mp030-003: WorktreeManager (git worktree jail)

> **Complexidade:** HIGH · **Depends on:** mp030-002 · **Fase:** 3  
> **BLUEPRINT-030** T-18–T-22 · §4.7 · §5.2 WorktreeManager

## Objetivo

Criar/remover worktrees sob `.dare/agent-worktrees/` via `SafeCommand` + `ProcessRunner`.

## Arquivos

- `crates/dare-agent/src/worktree.rs` (NOVO)
- `crates/dare-agent/src/lib.rs` (mod + re-exports)

## Contratos (MUST)

```text
AGENT_WORKTREES_REL = ".dare/agent-worktrees"
branch = dare/agent-{task_id}-{attempt}
rel_path = .dare/agent-worktrees/{task_id}-{attempt}
create: git worktree add -b <branch> <path> HEAD  (cwd=project root)
remove: git worktree remove --force <path>
```

IDs de task devem passar path-safety (jail); id inseguro → `Err`.

## Validation Gates

- [ ] `cargo test -p dare-agent` (MockProcessRunner argv + unsafe id)
- [ ] Opcional `#[ignore]` com git real
- [ ] Sem CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] `create` / `remove` / `list_orphans` / `cleanup_all` implementados
- [ ] Sem shell string concat — só argv

## Segurança

- RS-01 jail ids · RS-03 paths sob root · RS-05/06 ProcessRunner argv-only

## Próxima Task

mp030-004
