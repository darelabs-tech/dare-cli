# Task mp030-004: CLI `--agent` loop + Ralph on Done

> **Complexidade:** HIGH · **Depends on:** mp030-003 · **Fase:** 4  
> **BLUEPRINT-030** Apêndice C · T-04–T-17 · T-24 · T-33–T-35

## Objetivo

Wire `dare execute --agent` com loop fixed, worktrees, budget, cancel e Ralph-on-Done (029).

## Arquivos

- `crates/dare-cli/src/commands/execute_agent.rs` (NOVO)
- `crates/dare-cli/src/commands/execute.rs` / `main.rs` (flags + dispatch)
- `crates/dare-cli/Cargo.toml` (dep `dare-agent`)
- `crates/dare-cli/tests/cli_smoke.rs` (smokes `execute_agent_*`)

## Flags (MUST)

`--agent` exclusivo; `--driver` (default mock); `--task`; `--budget-tokens` (default 0); `--policy` (só `fixed`).

## Fluxo (MUST — Apêndice C)

1. Exclusivo · 2. exige `.git` else 4 · 3. load dag/state · 4. resolve driver · 5. policy fixed · 6. guard stub Ok · 7. select task · 8. budget · 9. loop ≤5 · Timeout→124 · budget→1 · Stop→1 · Done→complete 029 (ou skip se `DARE_AGENT_SKIP_RALPH=1`)

## Validation Gates

- [ ] `cargo test -p dare-cli --test cli_smoke -- execute_agent`
- [ ] Sem `--cleanup-worktrees` · Sem docs · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Smokes: success / fail-stop / timeout 124 / driver 4 / budget 1 / exclusive 2 / no-git 4
- [ ] Sem exit 6 · Sem drivers reais

## Segurança

- RS-08 cancel+cleanup · RS-09 guard stub only

## Próxima Task

mp030-005
