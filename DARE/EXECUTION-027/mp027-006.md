# Task mp027-006: Auditoria Ralph (fmt/clippy/test/audit)

> **Complexidade:** MED · **Depends on:** mp027-001, mp027-004, mp027-005 · **Fase:** 6  
> **BLUEPRINT-027** §7–8 · RS-04

## Objetivo

Gates de qualidade verdes para `dare-dag` (viz) + `dare-cli` (dag) antes do closeout.

## Descrição

Corrigir até passar fmt, clippy `-D warnings`, testes filtrados e audit/deny se aplicável.

## Validation Gates

- [ ] `cargo fmt --check`
- [ ] `cargo clippy -p dare-dag -p dare-cli --all-targets -- -D warnings`
- [ ] `cargo test -p dare-dag -- viz`
- [ ] `cargo test -p dare-cli --test cli_smoke -- dag`
- [ ] `cargo audit` (e `cargo deny check` se o repo exigir)
- [ ] Sem CVE HIGH/CRITICAL em deps novas
- [ ] Sem git commit · Sem execute 028+

## Testes

```bash
cargo fmt --check
cargo clippy -p dare-dag -p dare-cli --all-targets -- -D warnings
cargo test -p dare-dag -- viz
cargo test -p dare-cli --test cli_smoke -- dag
cargo audit
```

## Definition of Done (ANTI-STUB)

- [ ] Todos os gates acima verdes (não “skipped” silenciosamente)
- [ ] Nenhum warning clippy deixado com `allow` injustificado novo

## Segurança

- RS-04 supply chain: audit limpo ou mitigação documentada

## Próxima Task

mp027-007
