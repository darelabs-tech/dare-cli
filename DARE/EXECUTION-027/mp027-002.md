# Task mp027-002: viz Mermaid core + goldens

> **Complexidade:** HIGH · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-027** §4–5.2 · T-01 T-07–T-10 T-13 T-14 T-25 T-27

## Objetivo

API `dare_dag::viz::render` com formato Mermaid determinístico + goldens.

## Descrição

Criar `crates/dare-dag/src/viz.rs`. Reusar `compute_ranks`. Mermaid: `flowchart TB` + subgraphs por rank; edges `dep --> task` após subgraphs. Truncate título 40 chars; sanitize ids (`-`→`_`).

## Arquivos a Criar/Modificar

- `crates/dare-dag/src/viz.rs` (NOVO)
- `crates/dare-dag/src/lib.rs` (`mod viz`; pub use `VizFormat`, `VizOptions`, `render`)
- `tests/fixtures/dag/viz/sample.v21.yaml`
- `tests/fixtures/dag/viz/sample.mermaid.golden`
- testes unitários no crate (`#[cfg(test)]` em viz.rs ou `tests/`)

## Assinaturas (normativas)

```rust
pub enum VizFormat { Mermaid, Dot, Excalidraw }
impl VizFormat {
    pub fn as_str(&self) -> &'static str;
    pub fn parse(s: &str) -> Option<Self>; // exact lowercase only
}

pub struct VizOptions {
    pub title_max: usize,              // default 40
    pub state: Option<RuntimeStateV1>, // None => PENDING visuals (Fase 3)
}

pub fn render(
    doc: &DagDocument,
    format: VizFormat,
    opts: &VizOptions,
) -> Result<String, DagGraphError>
```

## Edge cases

- Ciclo / missing dep → `DagGraphError` (não panic)
- Título > 40 Unicode → truncar + `…`
- Id kebab `task-001` → alias Mermaid `task_001`
- EOL Windows nos goldens: normalizar `\r\n` → `\n` no assert
- **Não** emitir `subtask_prompt` no Mermaid (RS-02)

## Validation Gates

- [ ] `cargo test -p dare-dag -- viz` verde
- [ ] Golden Mermaid match após normalizar EOL
- [ ] Sem CLI / sem DOT+Excalidraw completos (Fase 3)
- [ ] Sem git commit

## Testes (MUST)

| Nome | Comportamento |
|------|----------------|
| `render_mermaid_matches_golden` | body == golden normalizado |
| `render_mermaid_cycle_errors` | `Err` com Cycle |
| `render_title_truncates` | título longo → `…` |
| `render_sanitize_kebab_id` | `task-a` → `task_a` no grafo |

```bash
cargo test -p dare-dag -- viz
```

## Definition of Done (ANTI-STUB)

- [ ] Nenhum `TODO`, `FIXME`, `XXX`, `HACK` em arquivos modificados
- [ ] Nenhuma função pública vazia / `unimplemented!()` / `todo!()` no path Mermaid
- [ ] Mocks só em testes
- [ ] `render(..., Mermaid, …)` produz flowchart real (não string fixa genérica sem nós)

## Segurança

- RS-02: sem prompts no output
- RS-03: truncate + escape labels Mermaid (`"` / `<` conforme necessário)

## Próxima Task

mp027-003
