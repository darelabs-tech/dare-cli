# Task mp027-001: Verificar docker-compose.ci.yml

> **Complexidade:** LOW · **Depends on:** — · **Fase:** 1  
> **BLUEPRINT-027** §6 Fase 1 · T-24

## Objetivo

Compose CI parseável; sem imagem nova para o microplano 027.

## Descrição

Validar `docker-compose.ci.yml` + presença de `Dockerfile.rust`. Sem código de domínio viz/CLI.

## Arquivos

- Ler: `docker-compose.ci.yml`, `Dockerfile.rust`
- Waiver (se Docker ausente): documentar em `cli-dag-viz.md` (mp027-005)

## Validation Gates

- [ ] `docker compose -f docker-compose.ci.yml config` exit 0 **ou** waiver registado
- [ ] Sem git commit · Sem `viz.rs` / `commands/dag.rs`

## Definition of Done (ANTI-STUB)

- [ ] Sem stubs · Sem git commit
- [ ] Nenhum `TODO`/`FIXME` introduzido

## Testes

```bash
docker compose -f docker-compose.ci.yml config
```

## Segurança

- Sem secrets · Sem shell arbitrário além do compose config

## Próxima Task

mp027-002 (paralela no rank 0) / mp027-006 depende desta.
