# Task mp027-003: viz DOT + Excalidraw + cores + OUTPUT_CAP

> **Complexidade:** HIGH · **Depends on:** mp027-002 · **Fase:** 3  
> **BLUEPRINT-027** §5.3–5.4 · T-11 T-12 T-15–T-17 T-20 RS-07

## Objetivo

Completar `render` para DOT e Excalidraw com cores complexity/status e teto `OUTPUT_CAP`.

## Descrição

Mesma ordem de nós/edges que Mermaid. DOT digraph; Excalidraw JSON determinístico (sem `updated`/`seed`). Soft-fail status se `state` ausente.

## Arquivos

- Modificar: `crates/dare-dag/src/viz.rs`
- Criar: `tests/fixtures/dag/viz/sample.dot.golden`
- Criar: `tests/fixtures/dag/viz/sample.excalidraw.golden`

## Constantes

| Nome | Valor |
|------|-------|
| `OUTPUT_CAP` | `2_097_152` |
| `EXCAL_W` / `EXCAL_H` | 120 / 60 |
| `EXCAL_DX` / `EXCAL_DY` | 200 / 100 |

## Cores

- Complexity fills: LOW `#e3f2fd`, MED `#fff3e0`, HIGH `#fce4ec`, unknown `#eeeeee`
- Status strokes: PENDING `#9e9e9e`; RUNNING `#1976d2` dashed; DONE `#2e7d32`; FAILED `#c62828`; SKIPPED `#757575` dashed
- `opts.state == None` → todos PENDING visuals (sem erro)

## Edge cases

- `body.len() > OUTPUT_CAP` → `DagGraphError::InvalidDag { message: "viz output too large" }`
- Labels DOT: escapar `"` e `\`
- Ids Excalidraw estáveis: `task-{id}`, `arrow-{from}-{to}`, `text-{id}`
- JSON Excalidraw parseável com `serde_json`

## Validation Gates

- [ ] Goldens DOT + Excalidraw passam (EOL normalizado)
- [ ] `cargo test -p dare-dag -- viz` verde
- [ ] Sem CLI · Sem git commit

## Testes (MUST)

| Nome | Comportamento |
|------|----------------|
| `render_dot_matches_golden` | DOT == golden |
| `render_excalidraw_matches_golden` | JSON == golden (ou subset estável) |
| `render_excalidraw_complexity_colors` | fills por complexity |
| `render_status_soft_without_state` | state None → PENDING visuals |
| `render_output_cap_errors` | oversize → InvalidDag message exata |

```bash
cargo test -p dare-dag -- viz
```

## Definition of Done (ANTI-STUB)

- [ ] Nenhum `TODO`/`FIXME`/`unimplemented!()` nos arms Dot/Excalidraw
- [ ] Nenhum return `"{}"` / string vazia como única saída de format público
- [ ] Mocks só em testes
- [ ] Cada edge case acima tem teste

## Segurança

- RS-07 OUTPUT_CAP
- RS-02 sem prompts nos elementos
- Sem paths absolutos / PII no JSON

## Próxima Task

mp027-004
