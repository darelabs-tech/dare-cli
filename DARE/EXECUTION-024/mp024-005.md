# Task mp024-005: CLI --ai/--provider + DesignReport v2 + smokes

> **Complexidade:** MED · **Depends on:** mp024-004 · **Fase:** 5  
> **BLUEPRINT-024** §5.7–5.12 · T-08 T-10 · RS-03

## Objetivo

Ligar enrichment opcional a `dare design` e subir `DesignReport` para schema 2.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-cli/Cargo.toml` (+ dare-ai) |
| MOD | `crates/dare-cli/src/main.rs` |
| MOD | `crates/dare-cli/src/commands/design.rs` |
| MOD | `crates/dare-cli/tests/cli_smoke.rs` |

## Contratos

- `DESIGN_SCHEMA_VERSION = 2`
- Campos: `ai: bool`, `provider: Option<String>`, `enriched: bool`
- Clap: `--ai`, `--provider`; Usage `"--provider requires --ai"`
- Pipeline BLUEPRINT §5.7 (write1 → enrich → write2; fail keeps write1)
- Default provider com `--ai` sem flag = **codex**
- Atualizar smokes que assertavam schemaVersion **1** → **2**

## Smokes / testes MUST

| Teste | Assert |
|-------|--------|
| `design_without_ai_schema_v2` | v2; ai false; enriched false |
| `design_ai_mock_enriches` | `--ai --provider mock` exit 0; enriched true |
| `design_provider_without_ai_usage` | exit 2 |
| `design_unknown_provider` | exit 4 |
| `design_ai_schema_fail_keeps_file` | write1 preservado; exit ≠ 0 |

## Validation Gates

- [ ] `cargo test -p dare-cli -- design`
- [ ] `cargo test -p dare-cli --test cli_smoke -- design`
- [ ] Sem `dare ai` (050) · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Comando real com enrich mock
- [ ] Falha não corrompe ficheiro
- [ ] Sem git commit
