# Task mp024-003: MockProvider + resolve_provider

> **Complexidade:** MED · **Depends on:** mp024-002 · **Fase:** 3  
> **BLUEPRINT-024** §5.1 §5.3 · T-03 T-05 T-06

## Objetivo

Trait `AiProvider` sync + `MockProvider` determinístico + `resolve_provider` (mock OK; outros not implemented até mp024-004 ligar Codex).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR/MOD | `crates/dare-ai/src/mock.rs` |
| MOD | `crates/dare-ai/src/provider.rs` |
| MOD | `crates/dare-ai/src/lib.rs` |

## Contratos

```rust
pub trait AiProvider: Send + Sync {
    fn id(&self) -> ProviderId;
    fn enrich(&self, req: &EnrichRequest) -> CoreResult<EnrichRaw>;
}

pub fn resolve_provider(id: ProviderId) -> CoreResult<Box<dyn AiProvider>>;
```

- `MockProvider`: JSON estável; `description` = eco de `req.description`; sem spawn
- `resolve_provider(Mock)` → `Ok`
- `resolve_provider(Codex|ClaudeCode|CursorCli|AntigravityCli)` → `Err(invalid_input("provider not implemented: …"))` **até** mp024-004 alterar Codex

## Testes MUST

| Teste | Assert |
|-------|--------|
| `mock_enrich_deterministic` | duas chamadas iguais; parse_and_validate OK |
| `resolve_unimplemented_provider_errors` | claude-code / cursor / antigravity Err |

## Validation Gates

- [ ] `cargo test -p dare-ai`
- [ ] Sem CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Mock real (não empty enrich)
- [ ] Sem `todo!()` nos paths públicos
- [ ] Sem git commit
