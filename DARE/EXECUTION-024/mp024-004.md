# Task mp024-004: CodexCliProvider + overrides + timeout/stdin

> **Complexidade:** HIGH · **Depends on:** mp024-003 · **Fase:** 4  
> **BLUEPRINT-024** §5.2 §5.4 · T-04 T-11 T-13 T-16 · RS-06

## Objetivo

Provider CLI real `codex` com overrides env, timeout 20 min, caps, redact; ligar `resolve_provider(Codex)`.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-ai/src/codex.rs` |
| CRIAR | `crates/dare-ai/src/redact_log.rs` |
| MOD | `crates/dare-ai/src/provider.rs` (`resolve` Codex) |
| MOD (opcional) | `crates/dare-core/src/process/{command,runner}.rs` — `.stdin(Vec<u8>)` |

## Contratos

- `parse_argv_override` — whitespace split
- `CodexCliProvider::from_env` — `DARE_CODEX_COMMAND` ou `codex` + `["exec"]`
- `enrich`: `SafeCommand` + `ENRICH_TIMEOUT` + caps; stdin **ou** prompt file `.dare/enrich-prompt-<pid>.txt` sob jail + delete
- Timeout/exit 124 → Err; exit ≠ 0 → Err com stderr `redact`+truncate 512
- `resolve_provider(ProviderId::Codex)` → `CodexCliProvider`

## Testes MUST

| Teste | Assert |
|-------|--------|
| `argv_override_split` | program+args |
| `codex_default_program_is_codex` | from_env sem env |
| `codex_uses_mock_runner_or_argv_assert` | MockProcessRunner / argv |
| (se aplicável) timeout → Err | |

## Validation Gates

- [ ] `cargo test -p dare-ai`
- [ ] Se patch core: `cargo test -p dare-core -- process`
- [ ] Sem flags CLI design · Sem git commit

## Segurança

- RS-02 redact · RS-05 sem API key · RS-06 argv-only

## Definition of Done (ANTI-STUB)

- [ ] Spawn path real via `SafeCommand` (testável com mock runner)
- [ ] Sem `todo!()` / silent Ok vazio
- [ ] Sem git commit
