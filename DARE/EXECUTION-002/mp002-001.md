# TASK 001: Docker Rust smoke (Dockerfile + Compose)

> **Complexidade:** LOW | **Depends on:** — | **Estimativa:** 0,5 h  
> **Microplano:** 002 | **Fase Blueprint:** 1

## 1. OBJETIVO

Ficheiros `Dockerfile.rust`, `docker-compose.rust.yml` e `.env.rust.example` existem; `docker compose -f docker-compose.rust.yml config` valida.

## 2. CONTEXTO

- BLUEPRINT-002 §6 Fase 1
- Healthcheck real só após binário (mp002-007); nesta task só scaffolding

## 3. ARQUIVOS

| Ação | Caminho |
|------|---------|
| CRIAR | `Dockerfile.rust` |
| CRIAR | `docker-compose.rust.yml` |
| CRIAR | `.env.rust.example` |

## 4. IMPLEMENTAÇÃO

Multi-stage conforme Blueprint (rust:1.85.0-bookworm → debian:bookworm-slim).  
Compose: serviço `dare-cli-smoke`, healthcheck `["CMD", "dare", "--version"]`.  
`.env.rust.example`: apenas `RUST_LOG=` comentado/vazio.

### Testes
- [ ] `docker compose -f docker-compose.rust.yml config` exit 0
- [ ] Ficheiros não contêm secrets

## 5. SEGURANÇA

- [ ] Sem secrets no Dockerfile
- [ ] Imagens oficiais pinadas por tag 1.85.0

## 6. VALIDATION GATES

```bash
docker compose -f docker-compose.rust.yml config
# Ralph (após mp002-009): cargo build/test/clippy — nesta task ainda pode falhar se crates incompletos
```

Até `dare.config` ser rust-axum, validar só compose config.

## 7–8. ANTI-STUB / DONE

- [ ] Healthcheck não é `CMD true`
- [ ] Atualizar `DARE/TASKS-002-workspace-rust-e-toolchain.md` → mp002-001 DONE

## 9. PRÓXIMA

`mp002-011` (após 010)
