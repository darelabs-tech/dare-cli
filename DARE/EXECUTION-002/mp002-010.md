# TASK 010: cargo audit + checklist RS-*

> **Complexidade:** HIGH | **Depends on:** mp002-008, mp002-009 | **Estimativa:** 1 h

## 1. OBJETIVO

`cargo audit` sem HIGH/CRITICAL; RS-01…RS-08 documentados; sem `[build] target` global; `.env.rust.example` limpo.

## 4. IMPLEMENTAÇÃO

Instalar `cargo-audit` se necessário (`cargo install cargo-audit --locked`).  
Secção Security gate 002 em `docs/compatibility/rust-msrv.md`.

## 6. GATES

```bash
cargo audit
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

## 8. DONE

- [ ] TASKS-002: mp002-010 DONE

## 9. PRÓXIMA

`mp002-011`
