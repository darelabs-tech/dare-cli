# TASK 007: Crate dare-cli --help/--version + smoke tests

> **Complexidade:** HIGH | **Depends on:** mp002-003,004,005,006 | **Estimativa:** 2 h

## 1. OBJETIVO

Binário `dare` responde `--help` e `--version` (`dare 0.1.0-alpha.0`); `cargo test --workspace` e `cargo build --release -p dare-cli` exit 0; teste de dep-graph garante libs ≠ dependem de `dare-cli`.

## 2. CONTEXTO

BLUEPRINT-002 §5.0–5.1, §5.6

## 3. ARQUIVOS

| Ação | Caminho |
|------|---------|
| MODIFICAR | `crates/dare-cli/Cargo.toml` |
| MODIFICAR | `crates/dare-cli/src/main.rs` |
| CRIAR | `crates/dare-cli/tests/cli_smoke.rs` |
| CRIAR | `crates/dare-cli/tests/dep_graph.rs` (ou equivalente) |

## 4. IMPLEMENTAÇÃO

Clap conforme §5.1. Zero args → print_help + exit 0.  
`RUST_LOG` opcional para tracing-subscriber.  
Testes listados no Blueprint.  
Dep-graph: metadata JSON — nenhuma lib dare-* tem dep `dare-cli`.

### Edge cases
- `--not-a-real-flag` → exit ≠ 0
- stdout version regex `^dare 0\.1\.0-alpha\.0\s*$`

## 5. SEGURANÇA

- [ ] RS-01 clap; RS-02 sem secrets

## 6. GATES

```bash
cargo build --workspace
cargo build --release -p dare-cli
cargo test --workspace
cargo run -p dare-cli -- --version
```

## 7–8. DONE

- [ ] Sem subcommands inventados
- [ ] TASKS-002: mp002-007 DONE

## 9. PRÓXIMA

`mp002-008`, `mp002-009`
