# Task mp051-001: Crate dare-server + config/auth/middleware/health

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Ao final, `crates/dare-server` compila no workspace com `create_app` servindo `GET /health`, auth Bearer/loopback, body limit 413 e security headers.

## Contexto

- **Fase:** A — BLUEPRINT-051 §0.1–0.3, §2, §3, §5.2, §5.14 (`create_app`)
- **DEC:** DEC-052 (docs na 007)

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `Cargo.toml` (workspace member + deps axum/tokio/tower-http/tower/http-body-util) |
| CRIAR | `crates/dare-server/Cargo.toml` |
| CRIAR | `crates/dare-server/src/lib.rs` |
| CRIAR | `crates/dare-server/src/{mode,config,state,error,auth,middleware,app}.rs` |
| CRIAR | `crates/dare-server/src/routes/{mod,health}.rs` |

## MUST

- Pins: `axum=0.8.8`, `tokio=1.45.1`, `tower-http=0.6.8`, `tower=0.5.2`, `http-body-util=0.1.3`
- **Sem** deps `dare-ai` / `dare-agent`
- `AppMode::{Dashboard,Rest}`
- `ServerConfig` + env `DARE_MCP_*` (§0.1); defaults bind `127.0.0.1`, ports 4100/3000, body `1048576`
- Auth §0.3 + `force_auth` para testes
- Error JSON `{error,code}`; 413 `body_too_large`; 401 `unauthorized`
- Headers: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, CSP const
- `GET /health` → `{ok,version,protocol:"rest",mode}`

## Testes

- `health_ok`
- `auth_force_require_401`
- `body_too_large_413`
- `headers_present`

## Validation Gates

```bash
cargo test -p dare-server
cargo clippy -p dare-server --all-targets -- -D warnings
```

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()` / stubs vazios
- [ ] Oneshot real contra Router
- [ ] Cada status code acima testado
- [ ] `dare review mp051-001` limpo
