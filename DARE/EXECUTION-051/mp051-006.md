# Task mp051-006: CLI dare dashboard/server + http contract smokes

> **Complexity:** HIGH · **Depends on:** mp051-003, mp051-004, mp051-005 · **Rank:** 3

## Objetivo

Ao final, `dare dashboard` e `dare server --protocol rest` existem no CLI com exits §5.15 e suite HTTP/CLI verde.

## Contexto

- **Fase:** D — BLUEPRINT §5.15–5.16

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-cli/src/commands/dashboard.rs` |
| CRIAR | `crates/dare-cli/src/commands/server.rs` |
| MOD | `crates/dare-cli/src/commands/mod.rs` |
| MOD | `crates/dare-cli/src/main.rs` |
| MOD | `crates/dare-cli/Cargo.toml` (`dare-server` dep) |
| CRIAR | `crates/dare-cli/tests/dashboard_cli.rs` |
| CRIAR/MOD | `crates/dare-server/tests/http_contracts.rs` |

## MUST

```text
dare dashboard [--port <u16>] [--no-open] [-d <dir>]
dare server --protocol rest [--bind <ip>] [--port <u16>] [-d <dir>]
```

| Caso | Exit |
|------|------|
| protocol ≠ `rest` | 2 |
| root missing | 3 |
| invalid bind/port | 4 |
| bind IO | 5 |

- Defaults: dashboard `127.0.0.1:4100`; rest `:3000`
- Contracts: happy + 403 escape + 401 `force_auth` + 413

## Testes CLI

- `help_lists_dashboard_server`
- `server_bad_protocol_exit_2`

## Validation Gates

```bash
cargo test -p dare-server
cargo test -p dare-cli --test dashboard_cli
cargo clippy -p dare-server -p dare-cli --all-targets -- -D warnings
```

## Definition of Done (ANTI-STUB)

- [ ] Help lista ambos comandos
- [ ] Thin CLI (lógica HTTP em `dare-server`)
- [ ] Contracts cobrem aceite Design O-06/O-07/O-08
