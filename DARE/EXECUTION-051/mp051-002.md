# Task mp051-002: Dashboard static assets (HTML/CSS/JS)

> **Complexity:** LOW · **Depends on:** — · **Rank:** 0

## Objetivo

Ao final, `crates/dare-server/assets/dashboard/` contém UI vanilla que consome `/api/telemetry` sem CDN e sem `innerHTML` com dados crus.

## Contexto

- **Fase:** B (assets) — BLUEPRINT T-04 / §3
- Paralelo com mp051-001 (criar diretórios mesmo se crate ainda não existir)

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-server/assets/dashboard/index.html` |
| CRIAR | `crates/dare-server/assets/dashboard/styles.css` |
| CRIAR | `crates/dare-server/assets/dashboard/app.js` |

## MUST

- Título: DARE Dashboard
- `app.js` faz `fetch('/api/telemetry')` e renderiza seções `dag`, `gates`, `cost`, `bestOfN`, `guard`, `drift`
- Escape via `textContent` (não interpolar JSON em HTML cru)
- Sem CDN; CSS/JS externos (CSP-friendly)
- Sem código Rust nesta task

## Testes

- Revisão manual / smoke visual na 003; aqui garantir ficheiros presentes e referências `styles.css` / `app.js` corretas no HTML

## Definition of Done (ANTI-STUB)

- [ ] Três ficheiros não vazios e ligados entre si
- [ ] Sem scripts inline com secrets
- [ ] Sem `TODO` placeholder de UI falsa
