# Task mp051-003: Dashboard routes + embed + telemetry

> **Complexity:** HIGH · **Depends on:** mp051-001, mp051-002 · **Rank:** 1

## Objetivo

Ao final, `GET /dashboard`, `/assets/*` (anti-traversal) e `GET /api/telemetry` funcionam via rust-embed + `TelemetrySnapshot`.

## Contexto

- **Fase:** B — BLUEPRINT §4.3, §5.3–5.5
- Assets de mp051-002 embutidos

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-server/src/telemetry.rs` |
| CRIAR | `crates/dare-server/src/routes/dashboard.rs` |
| MOD | `crates/dare-server/src/app.rs` (montar rotas dashboard em ambos modos) |
| MOD | `crates/dare-server/src/lib.rs` |

## MUST

```rust
pub fn build_telemetry_snapshot(root: &ProjectRoot) -> CoreResult<TelemetrySnapshot>;
```

- Embed `assets/dashboard/**`
- `/assets/{*path}`: `..` / absoluto / `\` / ext fora allowlist → **403**
- Telemetry: dag counts de `.dare/state.json` ou zeros + `source:"none"`; `drift.available` bool
- Dashboard **read-only** (sem PUT)

## Testes

- `dashboard_html_200`
- `assets_traversal_403`
- `assets_exe_forbidden`
- `telemetry_maps_keys`

## Validation Gates

```bash
cargo test -p dare-server
cargo clippy -p dare-server --all-targets -- -D warnings
```

## Definition of Done (ANTI-STUB)

- [ ] Embed real (não FS solto em prod path)
- [ ] 403 path escape testado
- [ ] Snapshot usa `dare_contracts::TelemetrySnapshot`
- [ ] Sem `todo!()`
