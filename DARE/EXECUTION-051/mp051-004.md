# Task mp051-004: REST routes (tools/context/dag/tasks/graph/steering/project)

> **Complexity:** HIGH · **Depends on:** mp051-001 · **Rank:** 1

## Objetivo

Ao final, modo `Rest` expõe a superfície HTTP legado §5.6–5.13 com path-safety, graph 503 e PUT tasks atômico.

## Contexto

- **Fase:** C + parte D — BLUEPRINT §0.4, §4.4–4.6, §5.6–5.13
- Paralelo com mp051-003

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-server/src/tasks_md.rs` |
| CRIAR | `crates/dare-server/src/routes/{tools,context,blueprint,dag,tasks,graph,project,steering}.rs` |
| MOD | `crates/dare-server/src/app.rs` (rotas só se `AppMode::Rest`) |
| MOD | `crates/dare-server/src/routes/mod.rs` |

## MUST

| Rota | Comportamento chave |
|------|---------------------|
| `GET /tools` | 12 tools ordem congelada §5.6 |
| `POST /context/query` | types fechados; query 1..=512 |
| `GET /blueprint` | jail; >2MiB → 400 |
| `GET /dag` | `load_dag` JSON |
| `GET\|PUT /tasks/:id` | regex id; emoji STATUS §4.5; PUT só Rest |
| `POST /graph/*` | locate / traverse / map-requirement; 503 se graph fail |
| `GET /project` | snapshot sem dep `dare-cli` |
| `GET /steering?file=` | `.env*` → 403 |

```rust
pub fn get_task_view(root: &ProjectRoot, id: &str) -> CoreResult<TaskView>;
pub fn put_task_status(root: &ProjectRoot, id: &str, status: &str) -> CoreResult<TaskView>;
```

## Testes

- `tools_order`
- `context_bad_type`
- `steering_env_403`
- `put_task_roundtrip`
- `graph_empty_query_400`
- `path_id_escape_403`
- Dashboard mode: PUT ausente → 404

## Validation Gates

```bash
cargo test -p dare-server
cargo clippy -p dare-server --all-targets -- -D warnings
```

## Definition of Done (ANTI-STUB)

- [ ] Handlers reais (sem `unimplemented!`)
- [ ] Cada erro HTTP da tabela testado
- [ ] PUT usa `atomic_write`
- [ ] Zero mutação no modo Dashboard
