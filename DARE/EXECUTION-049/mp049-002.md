# Task mp049-002: bench FixRate + suite loader + fixtures

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Fórmula Fix·Rate (§0.4), loader `suite.json`, baseline drop_pp e fixture `fixtures/bench`.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-verify/src/bench/{mod,suite,baseline}.rs` (+ fixrate helper) |
| CRIAR | `fixtures/bench/suite.json` |
| CRIAR | `fixtures/bench/cases/sample-ok/**` |

## MUST

- `ptp_failed > 0` → `fixtureFixRate = 0.0`
- `suite.json` schemaVersion 1; case dirs com 4 artefactos obrigatórios
- Inválido → `CoreError::usage` com `invalid bench suite`
- Sem CLI `dare bench`

## Testes

- `fixrate_ptp_zero`
- `fixrate_mean`
- `suite_invalid_missing_file`
- `baseline_drop`

## Definition of Done (ANTI-STUB)

- [ ] Fórmula coberta por unit
- [ ] Fixture sample-ok presente e parseável
