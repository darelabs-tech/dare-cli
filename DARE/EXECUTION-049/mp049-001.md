# Task mp049-001: reports + fail-to-pass + anti-tamper

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Tipos `AdvancedAspect` / `AspectResult` / `LoopVerdict` e aspectos fail-to-pass + anti-tamper (BLUEPRINT-049 Fase A).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-verify/src/report.rs` |
| CRIAR | `crates/dare-verify/src/aspects/mod.rs` |
| CRIAR | `crates/dare-verify/src/aspects/fail_to_pass.rs` |
| CRIAR | `crates/dare-verify/src/aspects/anti_tamper.rs` |
| MOD | `crates/dare-verify/src/lib.rs` |

## MUST

- Não alterar `GateAspect::{Build,Test,Lint}`
- `LoopVerdict` schemaVersion **1** camelCase (§4.1)
- FTP: lista ausente → `skipped` / `no_ftp_list`
- Anti-tamper: `removed_tests` per §0.9
- Sem mutation/formal/bench CLI

## Testes

- `aspect_status_serde`
- `ftp_all_pass`
- `ftp_missing_skipped`
- `anti_tamper_removed_tests`

## Definition of Done (ANTI-STUB)

- [ ] Asserts reais; sem `todo!()` nas APIs públicas
- [ ] Serde roundtrip LoopVerdict mínimo
