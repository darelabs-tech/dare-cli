# Task mp049-007: CLI dare bench + smokes

> **Complexity:** MED · **Depends on:** mp049-002 · **Rank:** 1

## Objetivo

Expor `dare bench` com exits e `--json` (§5.1).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-cli/src/commands/bench.rs` |
| MOD | `main.rs` / `commands/mod.rs` / `Cargo.toml` se preciso |
| CRIAR | `crates/dare-cli/tests/bench_cli.rs` |

## MUST

```text
dare bench [--suite] [--json] [--baseline] [--fail-on-regression N] [--filter] [-d]
```

- Suite inválida → exit **2**
- `--fail-on-regression` sem baseline → exit **2**
- Regressão `drop_pp > N` → exit **1**
- `--json` → `schemaVersion: 1`

## Testes (CLI)

- `suite_invalid_exit_2`
- `fail_on_regression_needs_baseline`
- `json_schema_version`
- `regression_exit_1`

## Definition of Done (ANTI-STUB)

- [ ] Smokes com exit codes reais
- [ ] Help lista `bench`
