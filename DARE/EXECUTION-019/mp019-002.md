# Task mp019-002: Tipos InstallPlan/Report + plan_install + select_ide + conflicts

> **Complexidade:** HIGH · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-019** §4 §5.1–5.2

## Objetivo

Tipos schema 1, `select_ide`, `plan_install` com política conflicts (warn / `--strict-conflicts`).

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-project/Cargo.toml` (+ dare-assets, dare-config, dare-contracts) |
| CRIAR | `crates/dare-project/src/install.rs` |
| MOD | `crates/dare-project/src/lib.rs` |

## Contratos

- `INSTALL_SCHEMA_VERSION = 1`; `INSTALL_READ_CAP = 262_144`
- `InstallOptions { force, dry_run, strict_conflicts }`
- `plan_install`: root null → InvalidInput; strict+conflicts → InvalidInput; else warnings
- `select_ide` §4.6 Blueprint
- 11 step ids canónicos

## Testes

`select_ide_defaults_claude_code`; `select_ide_hybrid`; `plan_strict_conflicts_errors`; `plan_conflicts_warns`; `install_report_schema_version_1`

## Validation Gates

- [ ] `cargo test -p dare-project`
- [ ] Sem git commit · Sem apply FS · Sem CLI

## Definition of Done (ANTI-STUB)

- [ ] Funções reais (não stub)
- [ ] Sem `todo!()` em paths públicos
- [ ] Sem git commit
