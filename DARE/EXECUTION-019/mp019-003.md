# Task mp019-003: apply FS steps + journal/rollback + dry_run

> **Complexidade:** HIGH · **Depends on:** mp019-002 · **Fase:** 3  
> **BLUEPRINT-019** §5.3–5.5

## Objetivo

`apply_install` steps 1–5 + `SessionJournal` + `rollback`; dry_run zero writes.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD/CRIAR | `crates/dare-project/src/install.rs` (+ gitignore/templates helpers) |

## Steps MUST

1. `ensure_dirs` — `DARE/`, `DARE/EXECUTION/`, `.dare/`
2. `write_config` — preserve se existe && !force; backup se overwrite
3. `materialize_templates` — 6 embed → `templates/`
4. `write_graph` — só se `dare-graph.yml` ausente
5. `merge_gitignore` — bloco `# BEGIN DARE` … `# END DARE`

Steps 6–11: `skipped` placeholder até mp019-004.

## Testes

`apply_dry_run_zero_writes`; `apply_creates_config_dare_gitignore`; `apply_idempotent_second_run`; `merge_gitignore_block_stable`; `templates_copied_under_templates_dir`; `graph_created_once`; `rollback_restores_forced_config`; `ensure_dirs_preserves_existing_execution_files`

## Validation Gates

- [ ] `cargo test -p dare-project`
- [ ] Sem git commit · Sem harness real · Sem CLI

## Definition of Done (ANTI-STUB)

- [ ] Rollback real (restore + delete created)
- [ ] Dry-run sem writes
- [ ] Sem git commit
