# mp041-003 — CLI dare graph + smokes

## Objetivo
Superfície `dare graph ingest|query|stats|viz` additive.

## Entrega
- `crates/dare-cli/src/commands/graph.rs`
- Wire em `main.rs` + `mod.rs`
- dep `dare-graph` em dare-cli
- Smokes em `cli_smoke.rs`

## Critérios
- Help lista subcomandos
- Tempdir ingest → query → stats → viz exit 0
