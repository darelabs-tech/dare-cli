# mp041-001 — ingest.rs contentHash + símbolos regex

## Objetivo
Indexar arquivos por contentHash sha256 e símbolos via regex no crate `dare-graph`.

## Entrega
- `crates/dare-graph/src/ingest.rs`
- Re-export em `lib.rs`
- Deps `sha2` + `regex` no Cargo.toml do crate
- Testes unitários skip/index

## Critérios
- Incremental: mesmo hash → skipped_unchanged
- IDs canônicos file/code_symbol
- Edge `contains` file→symbol
- Path jail; SKIP_DIRS; caps de walk
