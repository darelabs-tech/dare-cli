# Task mp008-002: Re-export `env_overrides_from_vars_strict`

## Objetivo

Expor na API pública o parser strict (RF-18 / gap Blueprint §0.1).

## Descrição

### Assinaturas (já em `env.rs`)

```rust
pub fn env_overrides_from_vars<I>(vars: I) -> EnvOverrides;
pub fn env_overrides_from_vars_strict<I>(vars: I) -> CoreResult<EnvOverrides>;
pub fn env_overrides_from_os() -> CoreResult<EnvOverrides>;
```

### Alteração obrigatória

Em `crates/dare-config/src/lib.rs`:

```rust
pub use env::{
    env_overrides_from_os,
    env_overrides_from_vars,
    env_overrides_from_vars_strict,
};
```

### Comportamento (não regressar)

| Input | Lenient | Strict |
|-------|---------|--------|
| `DARE_GUARD_ENABLED=maybe` | ignora | `Err` com `/env/DARE_GUARD_ENABLED` |
| Mensagem de erro | — | **sem** substring `maybe` (RS-02) |

Allowlist inalterada: `DARE_IDE`, `DARE_{PROJECT,AGENT,GUARD,GRAPH,HOOKS}_ENABLED`.

## Arquivos

- `crates/dare-config/src/lib.rs` (EDIT)
- `crates/dare-config/src/env.rs` (rustdoc)

## Validation Gates

- [ ] `env_overrides_from_vars_strict` importável via `dare_config::`
- [ ] `cargo test -p dare-config env::` passa
- [ ] Erro strict sem valor raw

## Testes

```bash
cargo test -p dare-config env::
```

## Segurança

- RS-02 redact; RS-01 allowlist

## Definition of Done (ANTI-STUB)

- [ ] Re-export real (não só doc)
- [ ] Nenhum `todo!()` / `unimplemented!()`
- [ ] Teste `strict_rejects_bad_bool` cobre pointer

## Próxima Task

mp008-003 e mp008-004 (paralelas)
