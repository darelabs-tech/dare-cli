# Task mp008-005: Docs config-and-migrations + DEC-009

## Objetivo

Documentar contrato público e alinhar com DEC-009 (RF-16).

## Descrição

Expandir `docs/compatibility/config-and-migrations.md` com secções:

1. Precedência CLI > env > file > default + matriz P/B
2. Allowlist `DARE_*` + grammar bool
3. API tipada (lista §5 Blueprint) — lenient vs strict
4. `load_effective` / `NotFound` → defaults
5. Migration: plan / dry-run (`writes: false`) / apply (backup + atomic)
6. `schemaVersion` opt-in via `MigrateOptions.write_schema_version`
7. JSON Pointer: `/ide`, `/env/DARE_GUARD_ENABLED`
8. Segurança RS-01…RS-10 (checklist curto)
9. Paridade TS 3.18.1 (Classe A precedência; deep validation COULD fora)
10. Links: DEC-009, `disk-and-json-policy.md`, `persisted-contracts.md`

**DEC-009:** já em `docs/DECISION-LOG.md` — verificar coerência; **não** criar DEC-010.

## Arquivos

- `docs/compatibility/config-and-migrations.md` (EDIT)
- `docs/DECISION-LOG.md` (só se nota DEC-009 incompleta)

## Validation Gates

- [ ] Matriz P/B na doc
- [ ] Strict vs lenient documentados
- [ ] DEC-009 referenciado
- [ ] Sem placeholders TBD

## Testes

```bash
# revisão: grep
rg -n "schemaVersion|P1|strict" docs/compatibility/config-and-migrations.md
```

## Segurança

- Documentar que erros não ecoam valores de env

## Definition of Done (ANTI-STUB)

- [ ] Doc utilizável por implementador sem abrir só o código
- [ ] Exemplos concretos (não “ver código”)
- [ ] Sem inventar nova DEC

## Próxima Task

mp008-006
