# Task mp008-004: Matriz P/B + gates migration

## Objetivo

Congelar precedência P1–P5/B1–B3 e side-effects de migrate (O-01, O-04–O-06).

## Descrição

### 1) Precedência

Ficheiro: `crates/dare-config/tests/precedence.rs`

| ID | Esperado |
|----|----------|
| P1 | env.ide vence file |
| P2 | cli.ide vence file |
| P3 | cli vence env |
| P4 | sem overrides → ide None |
| P5 | só file |
| B1 | env habilita guard |
| B2 | cli vence env em block_enabled |
| B3 | extras do file preservados |

Completar testes em falta.

### 2) Migration gates

Em `migrate.rs` `#[cfg(test)]` (ou teste integração):

```rust
#[test]
fn apply_default_options_does_not_write_schema_version() { /* … */ }

#[test]
fn apply_empty_steps_skips_backup() { /* … */ }
```

**Regras:**
- `MigrateOptions::default()` → `write_schema_version == false` → `plan.steps` vazio → apply **não** grava `schemaVersion` e **não** cria `.dare/backups`
- Manter: `dry_run_does_not_write` (bytes bit-iguais); `apply_creates_backup_and_writes` (com flag true)

### Assinaturas

```rust
pub fn plan_migrate(current: &DareConfig, opts: &MigrateOptions) -> MigrationPlan;
pub fn dry_run_migrate(...) -> CoreResult<MigrateDryRunReport>; // writes: false
pub fn apply_migrate(...) -> CoreResult<MigrationPlan>;
```

## Arquivos

- `crates/dare-config/tests/precedence.rs`
- `crates/dare-config/src/migrate.rs` (testes)

## Validation Gates

- [ ] P1–P5 + B1–B3 verdes
- [ ] Default apply sem schemaVersion / sem backup
- [ ] Dry-run zero-write
- [ ] `cargo test -p dare-config`

## Testes

```bash
cargo test -p dare-config
```

## Segurança

- RS-08 backup só quando steps ≠ ∅ e ficheiro existia
- RS-10 dry-run nunca escreve

## Definition of Done (ANTI-STUB)

- [ ] Cada ID P/B com teste nomeado
- [ ] Asserts de ausência de backup (não só “passa”)
- [ ] Sem `todo!()`

## Próxima Task

mp008-005
