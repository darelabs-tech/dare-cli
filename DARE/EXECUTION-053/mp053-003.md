# Task mp053-003: SignatureVerifier + apply atomic + backup

> **Complexity:** HIGH · **Depends on:** mp053-002 · **Rank:** 2

## Objetivo

Ao final, assinatura fail-closed e `apply_update` fazem backup + replace atômico com testes de falha parcial.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-self/src/verify.rs` (`SignatureVerifier`) |
| CRIAR | `crates/dare-self/src/apply.rs` |

## MUST

```rust
pub trait SignatureVerifier {
  fn verify_sums(&self, sums: &Path, sig: &Path) -> CoreResult<()>;
}
// RejectSkipped / CosignCliVerifier (argv separado)
pub fn apply_update(opts: UpdateOpts, verifier: &dyn SignatureVerifier) -> CoreResult<ApplyReport>;
```

| Caso | Resultado |
|------|-----------|
| `.sig` prefix `signing skipped` | Err `MSG_SIGNING_SKIPPED` |
| cosign missing | Err `MSG_COSIGN_MISSING` |
| `DARE_SELF_ALLOW_UNSIGNED=1` | skip cosign após sha256 + stderr warning |
| apply pipeline | lock → verify → backup → atomic replace → smoke → cleanup |

## Testes

- `signing_skipped_rejected`
- `apply_failpoint_preserves_previous` (ou equivalente)

## Definition of Done (ANTI-STUB)

- [ ] Sem shell concatenado no cosign
- [ ] Backup antes do replace
