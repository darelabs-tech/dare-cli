# Task mp053-002: plan + download + SHA-256 verify

> **Complexity:** HIGH · **Depends on:** mp053-001 · **Rank:** 1

## Objetivo

Ao final, `plan_update` monta o plano e a verificação SHA-256 funciona com fixtures (sem rede).

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-self/src/{plan,download,verify}.rs` |
| CRIAR | `crates/dare-self/tests/fixtures/SHA256SUMS` (+ asset bytes) |
| MOD | `lib.rs` re-exports |

## MUST

```rust
pub fn plan_update(opts: UpdateOpts) -> CoreResult<UpdatePlan>; // schemaVersion 1
pub trait HttpClient { fn get_bytes(&self, url: &str) -> CoreResult<Vec<u8>>; }
pub fn verify_sha256(asset: &[u8], sums_text: &str, asset_name: &str) -> CoreResult<()>;
```

- Asset: `dare-${TAG}-${TARGET}.tar.gz|zip`
- `SHA256SUMS` formato two-space; mismatch → erro tipado (`MSG_CHECKSUM_MISMATCH`)

## Testes

- `plan_asset_name_windows_zip` / unix tar.gz
- `verify_sha256_ok` / `verify_sha256_mismatch`

## Definition of Done (ANTI-STUB)

- [ ] HttpClient mockável; zero calls GitHub reais nos unit tests
- [ ] Sem cosign/apply ainda
