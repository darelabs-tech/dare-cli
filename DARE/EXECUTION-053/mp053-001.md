# Task mp053-001: Crate dare-self — paths + lock + channel

> **Complexity:** MED · **Depends on:** — · **Rank:** 0

## Objetivo

Ao final, `crates/dare-self` existe no workspace com `SelfHome`, `Channel` e lock exclusivos testáveis.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-self/Cargo.toml` |
| CRIAR | `crates/dare-self/src/{lib,paths,channel,lock}.rs` |
| MOD | `Cargo.toml` (workspace members + deps path) |

## MUST

```rust
pub enum Channel { Beta, Stable }
impl Channel { pub fn parse(s: &str) -> Result<Self, /*typed*/>; }
pub struct SelfHome { /* root path */ }
pub fn acquire_lock(home: &SelfHome) -> Result<LockGuard, LockHeld>;
```

- Default home: `DARE_SELF_HOME` ou `~/.dare/self`
- Stale force helper se mtime > 3600s
- Constantes `DEFAULT_CHANNEL=beta`, msgs lock

## Testes

- `channel_parse_ok`, `channel_parse_bad`
- `lock_contention_second_fails`

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!()`; crate compila `cargo test -p dare-self`
- [ ] Sem download/apply/CLI
