# Task mp053-004: rollback + uninstall

> **Complexity:** MED · **Depends on:** mp053-003 · **Rank:** 3

## Objetivo

Ao final, `rollback` e `uninstall` cobrem o ciclo pós-update com blast radius mínimo.

## Arquivos

| Ação | Caminho |
|------|---------|
| CRIAR | `crates/dare-self/src/{rollback,uninstall}.rs` |
| MOD | `lib.rs` |

## MUST

```rust
pub fn rollback(opts: RollbackOpts) -> CoreResult<RollbackReport>;
pub fn uninstall(opts: UninstallOpts) -> CoreResult<UninstallReport>;
```

- Sem backup → `MSG_NO_BACKUP`
- Uninstall remove **somente** o path do binário alvo (fixture); não apaga `SelfHome` nem projetos

## Testes

- `rollback_restores_backup`
- `rollback_missing_backup_err`
- `uninstall_removes_only_exe`

## Definition of Done (ANTI-STUB)

- [ ] Sem `--purge` v1
- [ ] Testes FS reais (tempfile)
