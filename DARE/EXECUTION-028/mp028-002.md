# Task mp028-002: execution core (ready/compose/snapshot)

> **Complexidade:** HIGH · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-028** §4–5.5 · T-01 T-09–T-16 T-25 T-29

## Objetivo

API de domínio em `dare_dag::execution` para ready min-rank, compose de prompt e status snapshot.

## Arquivos a Criar/Modificar

- `crates/dare-dag/src/execution.rs` (NOVO)
- `crates/dare-dag/src/lib.rs`
- `tests/fixtures/dag/exec-empty.v21.yaml`
- `tests/fixtures/dag/exec-blocked.v21.yaml` (e/ou state inline)

## Assinaturas (normativas)

```rust
pub enum ExecuteOutcome { Status, NextReady, Resolved, Blocked, Empty, Waiting }
pub fn parent_context_limit(doc: &DagDocument) -> usize
pub fn ready_at_min_rank(doc, state, ranks) -> Vec<String>
pub fn compose_task_prompt(doc, state, task_id) -> Result<String, DagGraphError>
pub fn build_status_snapshot(doc, state, ranks) -> StatusSnapshot
pub fn classify_next_outcome(doc, state, ready: &[String]) -> ExecuteOutcome
```

Constantes: `MSG_RESOLVED`, `MSG_BLOCKED`, `MSG_EMPTY` (exatas Blueprint §0.2).

## Edge cases

- Tail: últimos N Unicode scalars; output vazio → omitir pai
- 0 tasks → Empty
- ready vazio + PENDING → Blocked
- ready vazio + RUNNING>0 → Waiting
- task_id ausente em compose → InvalidDag

## Validation Gates

- [ ] `cargo test -p dare-dag -- execution` verde
- [ ] Sem CLI · Sem `transition(Start)` · Sem git commit

## Testes (MUST)

| Nome | Comportamento |
|------|----------------|
| `ready_at_min_rank_filters` | só min rank |
| `compose_tail_exact_chars` | cap exacto |
| `compose_omits_empty_parent` | sem bloco vazio |
| `classify_empty_blocked_resolved_waiting` | 4 outcomes |
| `parent_context_legacy_2000` | Legacy → 2000 |

## Definition of Done (ANTI-STUB)

- [ ] Sem `todo!`/`unimplemented!` nas APIs públicas
- [ ] Mocks só em testes
- [ ] Cada edge case com teste

## Segurança

- RS-07 cap parent context

## Próxima Task

mp028-003
