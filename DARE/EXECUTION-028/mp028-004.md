# Task mp028-004: CLI `--watch` + read-only guarantee

> **Complexidade:** MED · **Depends on:** mp028-003 · **Fase:** 4  
> **BLUEPRINT-028** §5.6 watch · T-17–T-20 · RS-08

## Objetivo

`--watch` observa status sem mutar `.dare/state.json` nem canvas.

## Descrição

Loop com `--interval` (default 2) e `--max-ticks`. Cada tick: re-load DAG + soft-load state + snapshot. Proibido `ensure_state` / save / `canvas::write`.

## Validation Gates

- [ ] Smoke: `dare execute --watch --max-ticks 1` exit 0
- [ ] Teste FS: bytes/hash de `state.json` idênticos antes/depois
- [ ] Canvas não reescrito pelo watch
- [ ] Sem git commit

## Testes (MUST)

| Nome | Comportamento |
|------|----------------|
| `execute_watch_max_ticks_one` | exit 0, imprime snapshot |
| `watch_does_not_mutate_state` | hash state estável |

## Definition of Done (ANTI-STUB)

- [ ] Watch path real (não stub)
- [ ] Zero writes state/canvas no path watch
- [ ] Mocks só em testes

## Segurança

- RS-08 integridade sob cancel/watch

## Próxima Task

mp028-005
