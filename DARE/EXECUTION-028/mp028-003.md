# Task mp028-003: CLI `--status` + `--next` + smokes

> **Complexidade:** HIGH · **Depends on:** mp028-002 · **Fase:** 3  
> **BLUEPRINT-028** §5.6–5.8 · T-02–T-08

## Objetivo

Superfície `dare execute` com default `--status` e `--next`, ensure+canvas, JSON, exit codes.

## Arquivos

- `crates/dare-cli/src/commands/execute.rs` (NOVO)
- `crates/dare-cli/src/commands/mod.rs`
- `crates/dare-cli/src/main.rs`
- `crates/dare-cli/tests/cli_smoke.rs` (smokes `execute_*`)

## Assinatura

```rust
pub fn run_execute(
    dag: Option<PathBuf>,
    action: ExecuteAction,
    renderer: &OutputRenderer<'_>,
) -> ExitCode
```

## Fluxo Status/Next (MUST)

1. root → `resolve_project_rel` → `load_dag`
2. `ensure_state` + `compute_ranks` + `canvas::write`
3. **Sem** `transition(Start)`
4. Format human / `--json` §5.7

## Exit codes

| Code | Caso |
|------|------|
| 0 | OK incl. empty/resolved/blocked/waiting |
| 2 | `--status` + `--next` juntos |
| 3 | DAG missing |
| 4 | cycle / no root / config |
| 5 | lock Io |

## Validation Gates

- [ ] Smokes listados no YAML
- [ ] `cargo test -p dare-cli --test cli_smoke -- execute`
- [ ] Sem watch completo (defer 004) · Sem git commit · Sem 029 flags

## Definition of Done (ANTI-STUB)

- [ ] Clap real com ArgGroup
- [ ] Cada smoke MUST passa
- [ ] Sem `unimplemented!` em Status/Next

## Segurança

- RS-01 jail `--dag`
- RS-06 sem Ralph/shell

## Próxima Task

mp028-004
