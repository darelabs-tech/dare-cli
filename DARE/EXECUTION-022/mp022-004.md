# Task mp022-004: Migrations config + failpoint rollback

> **Complexidade:** HIGH · **Depends on:** mp022-003 · **Fase:** 4  
> **BLUEPRINT-022** §5.5 steps 8–9 · T-08 T-09 T-10

## Objetivo

Migrate de `dare.config.json` integrado no apply (session journal único); falha parcial restaura tree (exceto dir backup).

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-update/src/apply.rs` |

## Contratos

### `should_migrate`

- true se plan flag / item `dare.config.json` status ∈ {Missing, Apply} / (Customized && action Replace)
- customized + Keep → **não** migrar

### Migrate (não usar `apply_migrate` nested)

```text
session_backup(dare.config.json) se exists
cfg = load_or_default
plan_m = plan_migrate(&cfg, MigrateOptions { write_schema_version: true, schema_version: 1 })
after = apply_plan_in_memory(&cfg, &plan_m)
save_dare_config(...)
migrated.push("dare.config.json")
```

### Failpoint

```rust
#[cfg(test)]
pub(crate) fn apply_update_with_failpoint(
    root: &ProjectRoot,
    plan: &UpdatePlan,
    opts: ApplyOptions,
    after_n: usize,
) -> CoreResult<UpdateApplyReport>
```

Após `after_n` writes bem-sucedidos → `Err(CoreError::io(...))` → `rollback_session`.

## Testes

- `apply_migrate_writes_schema_version` — `extra.schemaVersion == 1`
- `apply_migrate_kept_customized_config_skips` — customized+yes → sem migrate write
- `apply_partial_failure_rolls_back` — conteúdo pré-apply restaurado; backup dir pode existir

## Validation Gates

```bash
cargo test -p dare-update
```

- [ ] Três testes MUST · extras de config preservados · Sem CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] Migrate real via `dare-config` in-memory + save
- [ ] Failpoint + rollback comprovado em teste
- [ ] Sem `todo!()` · Sem git commit

## Segurança

- [ ] Um journal de sessão · sem dual `.dare/backups` nested no happy path de update
- [ ] Sem secrets em warnings

## Próxima Task

`mp022-005` — CLI + smokes
