# Task mp022-002: Policy + session backup + journal/rollback

> **Complexidade:** MED · **Depends on:** — · **Fase:** 2  
> **BLUEPRINT-022** §4–5.4 · T-02…T-07 · T-10

## Objetivo

`resolve_action` cobre a matriz §5.1; `ensure_backup_root` / `session_backup_file` / `rollback_session` passam testes; `dare-update` depende de `dare-config`.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-update/Cargo.toml` (+ `dare-config`) |
| CRIAR | `crates/dare-update/src/policy.rs` |
| CRIAR | `crates/dare-update/src/session_backup.rs` |
| MOD | `crates/dare-update/src/lib.rs` |

## Contratos

### `resolve_action`

```rust
pub fn resolve_action(
    status: AssetUpdateStatus,
    opts: &ApplyOptions,
    batch_replace_customized: bool,
) -> ApplyAction
```

| status | force | yes | interactive | batch | → |
|--------|-------|-----|-------------|-------|---|
| identical | * | * | * | * | Keep |
| missing | * | * | * | * | Replace |
| apply | * | * | * | * | Replace |
| customized | true | * | * | * | Replace |
| customized | false | true | * | * | Keep |
| customized | false | false | false | * | Keep |
| customized | false | false | true | true | Replace |
| customized | false | false | true | false | Keep |

### Backup

- Root: `.dare/backup-{sanitizedVersion}` ou `.dare/backup-{ver}-{utc}` se colisão
- `APPLY_READ_CAP = 262_144`
- `UPDATE_APPLY_SCHEMA_VERSION = 1`
- Rollback: restore `backed_up` rev → delete `created` → rmdir empty `created_dirs`; **não** apagar backup root

### Deps

- ADD `dare-config`; **não** `dare-cli` / `dare-project` / `dare-dag`

## Testes

- `resolve_action_matrix_all_rows`
- `ensure_backup_root_creates_and_collides`
- `session_backup_and_rollback_restores`
- `read_cap_rejects_huge_backup_source` → `InvalidInput`

## Validation Gates

```bash
cargo test -p dare-update
```

- [ ] Matriz 100% · backup/rollback round-trip · Sem `apply_update` completo · Sem CLI · Sem git commit

## Definition of Done (ANTI-STUB)

- [ ] `resolve_action` real (sem stub)
- [ ] Backup/restore reais com `atomic_write`
- [ ] Sem `todo!()` / `unimplemented!()` em APIs públicas
- [ ] Sem git commit

## Segurança

- [ ] Paths só via `SafeRelativePath` / `ProjectRoot`
- [ ] Cap de leitura enforced

## Próxima Task

`mp022-003` — apply_update assets · `mp022-006` — docs (paralelo após esta)
