# Task mp022-005: CLI -y/--force + smokes apply

> **Complexidade:** MED · **Depends on:** mp022-004 · **Fase:** 5  
> **BLUEPRINT-022** §5.7 §5.11 · T-16

## Objetivo

`dare update` sem `--dry-run` aplica de verdade; `-y` preserva customized; `--force` sobrescreve; smokes §5.11 passam.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-cli/src/main.rs` |
| MOD | `crates/dare-cli/src/commands/update.rs` |
| MOD | `crates/dare-cli/tests/cli_smoke.rs` |

## Contratos CLI

```rust
Update {
    dir: Option<PathBuf>,      // -d/--dir
    dry_run: bool,             // --dry-run
    yes: bool,                 // -y/--yes
    force: bool,               // --force
    target: Option<String>,    // --target harness
}
```

| Fluxo | Comportamento |
|-------|---------------|
| `dry_run` | plan only (021); zero writes; force ignorado |
| `!dry_run` | plan → `apply_update` → `UpdateApplyReport` human/JSON |
| interactive | tty && !yes && !force → AskFn stdin batch |
| Ask prompt | stderr: `Replace all N customized files? [y/N]: ` |

**REMOVER** stub `Internal` “use --dry-run (022)”.

Exit codes: 0 ok; 3 dir missing; 4 invalid root/target; 5 Io.

## Smokes MUST

| Teste | Assert |
|-------|--------|
| `update_dry_run_zero_write` | listing unchanged |
| `update_yes_keeps_customized` | bytes same + warning JSON |
| `update_force_overwrites_customized` | canonical + `.dare/backup-*` |
| `update_creates_missing` | path created |
| `update_dir_missing_exit_3` | code 3 |

Preservar smokes dry-run existentes (021).

## Validation Gates

```bash
cargo test -p dare-cli --test cli_smoke -- update
```

- [x] Smokes §5.11 · dry-run 021 intactos · Sem git commit

## Definition of Done (ANTI-STUB)

- [x] Apply path real (não stub)
- [x] Ask reader real quando interactive
- [x] Sem `todo!()` · Sem git commit

## Segurança

- [x] `-y` ≠ `--force` demonstrado em smoke
- [x] Non-TTY sem hang em stdin

## Próxima Task

`mp022-007` — Ralph (após 001+006 também)
