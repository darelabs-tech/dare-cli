# Task mp048-005: CLI dare hooks list/run/validate

> **Complexity:** MED · **Depends on:** mp048-004 · **Rank:** 3

## Objetivo

Expor `dare hooks list|run|validate` no CLI com exits e `--json` (BLUEPRINT-048 §5.1 / §5.4).

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `crates/dare-cli/Cargo.toml` (dep `dare-hooks`) |
| MOD | `crates/dare-cli/src/main.rs` |
| MOD | `crates/dare-cli/src/commands/mod.rs` |
| CRIAR | `crates/dare-cli/src/commands/hooks.rs` |
| CRIAR/MOD | `crates/dare-cli/tests/hooks_steering_cli.rs` |

## MUST

```text
dare hooks list [--json] [-d]
dare hooks run <EVENT> [--file] [--task] [--trust] [--json] [-d]
dare hooks validate [--json] [-d]
```

- Unknown event → exit **2**
- Untrusted run → exit **2** + stderr contém `HOOKS_TRUST`
- Algum action `failed` → exit **1**; todos ok/skipped → **0**
- `validate` `ok=false` → exit **4**
- Mensagens en-US

## Testes (CLI smoke)

- `unknown_event_exit_2`
- `untrusted_exit_2`
- `validate_ok_json` (`schemaVersion == 1`)

## Validation Gates

- [ ] `cargo test -p dare-cli --test hooks_steering_cli`
- [ ] help contém `hooks`

## Definition of Done (ANTI-STUB)

- [ ] Smokes com `assert_cmd` / exit codes reais
- [ ] Sem stub `not implemented` no path CLI
