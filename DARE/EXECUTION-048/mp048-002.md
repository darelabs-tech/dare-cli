# Task mp048-002: dare-steering frontmatter + resolve + .env deny

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Criar a crate `dare-steering` com parse de frontmatter, resolução por priority/glob e exclusão obrigatória de `.env*` (BLUEPRINT-048 §0.7–0.8).

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `Cargo.toml` (`globset = "=0.4.16"`, member `dare-steering`) |
| CRIAR | `crates/dare-steering/Cargo.toml` |
| CRIAR | `crates/dare-steering/src/{lib,frontmatter,resolve,env_deny,report}.rs` |

## MUST

- `is_env_excluded(basename)`: `.env` ou prefixo `.env.`
- `list_steering` / `show_steering` assinaturas do BLUEPRINT §5.2
- DNA priority 0, PATTERNS priority 1; `.dare/steering/*.md` não recursivo
- Sort: priority ASC, path ASC (posix `/`)
- `show` em `.env*` → `InvalidInput` `MSG_ENV_EXCLUDED` **antes** de ler
- Path escape → InvalidInput; missing → NotFound
- Reports camelCase `schemaVersion: 1`
- Sem CLI neste task

## Testes

- `env_deny_dot_env`
- `env_deny_dot_env_local`
- `priority_sort`
- `glob_match`
- `show_excludes_env`

## Validation Gates

- [ ] `cargo test -p dare-steering`
- [ ] `cargo clippy -p dare-steering --all-targets -- -D warnings`

## Definition of Done (ANTI-STUB)

- [ ] Deny `.env*` coberto por teste (sem read do ficheiro)
- [ ] Glob + priority com asserts reais
- [ ] Sem `todo!()` nas APIs públicas
