# Task mp048-001: dare-hooks events + actions + defs/embed

> **Complexity:** HIGH · **Depends on:** — · **Rank:** 0

## Objetivo

Criar a crate `dare-hooks` com eventos fechados, allowlist de ações e carga de defs (embed + overlay) per BLUEPRINT-048 §0.2–0.4.

## Arquivos

| Ação | Caminho |
|------|---------|
| MOD | `Cargo.toml` (member + `dare-hooks` path dep workspace) |
| CRIAR | `crates/dare-hooks/Cargo.toml` |
| CRIAR | `crates/dare-hooks/src/lib.rs` |
| CRIAR | `crates/dare-hooks/src/event.rs` |
| CRIAR | `crates/dare-hooks/src/action.rs` |
| CRIAR | `crates/dare-hooks/src/defs.rs` |
| CRIAR | `assets/hooks/default-hooks.yml` |

## MUST

- `HookEvent` strings exact: `on-save`, `on-file-create`, `on-task-complete`, `pre-commit`
- Unknown event → `CoreError::usage` com `unknown hook event: {event}`
- `HookAction` + `action_argv`: validate / review / `graph ingest` / guard / info
- Embed via `include_str!(.../assets/hooks/default-hooks.yml)` (padrão dare-guard)
- Overlay `.dare/hooks.yml` **substitui** array `hooks` se presente
- `schemaVersion` MUST `1`
- Sem dep de `dare-cli`; sem `run_hooks` / CLI

## Testes

- `parse_events`
- `parse_actions`
- `load_embed`
- `overlay_replaces`
- `reject_unknown_action_in_overlay`

## Validation Gates

- [ ] `cargo test -p dare-hooks`
- [ ] `cargo clippy -p dare-hooks --all-targets -- -D warnings`

## Definition of Done (ANTI-STUB)

- [ ] Nenhum `todo!()` / função pública vazia
- [ ] Asserts reais nos 5 testes
- [ ] YAML embed parseia e valida ações
- [ ] Mocks só em testes
