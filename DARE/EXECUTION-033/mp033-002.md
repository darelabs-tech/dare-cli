# TASK mp033-002: spliceSubDag + depth/cycle/state

> **Complexidade:** HIGH  
> **Depends on:** mp033-001

## 1. OBJETIVO

`splice_sub_dag` + `apply_refine` com MaxDepth≤2, CycleError, parentId/dependsOn.

## 3. ARQUIVOS A CRIAR / MODIFICAR

| Ação | Caminho | Descrição |
|------|---------|-----------|
| MODIFICAR | `crates/dare-dag/src/subdag.rs` | splice + apply |
| USAR | `state.rs` / `validate.rs` / contracts | lock + save |

## 4. IMPLEMENTAÇÃO

- Chain children; rewire dependents → last child; remove parent from YAML
- State: children `parentId`; parent `SPLIT`; `dependsOn` synced
- Errors: `SubDagError::{Cycle,MaxDepth,TaskNotFound,Invalid}`
- Unit: rewire, depth block, cycle, parentId

## 7. ANTI-STUB

Apply deve escrever ficheiros reais em temp dir nos testes.
