# dare-reverse

CLI command: `reverse`

Harness output paths (from `assets/capability-matrix.yml`):

| Harness     | Path                                        |
|-------------|---------------------------------------------|
| Claude      | `.claude/commands/dare-reverse.md`          |
| Cursor      | `.cursor/commands/dare-reverse.md`          |
| Codex       | `.codex/skills/dare-reverse/SKILL.md`       |
| Antigravity | `.antigravity/commands/dare-reverse.md`     |

Static layer: `dare reverse` (deterministic IDEIA + REVERSE facts).  
Semantic layer: IDE `/dare-reverse` or `dare reverse --ai` (soft-fail).
