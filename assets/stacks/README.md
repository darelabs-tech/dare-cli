# Scaffold stack templates

MVP skeletons under `assets/stacks/<id>/` for the 11 supported scaffold IDs.

Each root includes `dare.config.json.tpl` plus idiomatic stub files (BLUEPRINT-046 §0.6).
Placeholders: `{{project_name}}`, `{{stack_id}}`.
