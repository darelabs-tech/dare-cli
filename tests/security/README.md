# Security suite

Regression fixtures for command injection, env leak, archive traversal,
signature mismatch, and unicode bidi controls.

Orchestrated by `dare-parity` (later mp054-004); path/archive SoT remains in
`dare-core`, `dare-skills`, and `dare-self`.
