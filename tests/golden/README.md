# Golden parity suite

Committed snapshots and `case.yaml` specs for TypeScript ↔ Rust contract parity.

Cases live under `cases/<id>/` with axes from `dare-parity::CompareAxis`.
Fixtures are under `tests/fixtures/`. See `docs/compatibility/parity-hardening.md`
(added in a later mp054 task) for the allowlisted normalizations and diff log.
