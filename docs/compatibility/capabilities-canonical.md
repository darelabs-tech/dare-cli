# Capabilities canónicas (ADR-007 / microplano 010)

- Fonte: `assets/capability-matrix.yml` (49 entries = `.claude/commands/*`)
- Tipos: `Capability`, `HarnessOutputs`, `validate_capability_matrix`, `render_claude_command`
- Crate: `dare-assets` (extração para `dare-harness` deferida aos adapters 011+)
- Exceções: lista vazia neste ciclo; Cursor rules/skills baseline incompletos no repo
