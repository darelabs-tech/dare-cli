# Phase 4 — Execute

The Execute phase is where **the AI implements tasks** with auto-correction via the Ralph Loop.

## Goal
Implement code and pass validation gates.

## Command
```bash
dare execute task-001
```
