# DAG Runner

The **DAG Runner** executes task graphs in parallel using **Kahn's algorithm** for topological sorting.

## Configuration File `dare-dag.yaml`
Specifies tasks, descriptions, statuses, and dependencies.
